# Per-run machine-readable evidence artifacts (regenerated by every run;
# an RC record cites a specific run's copy, checked in deliberately there).
results/
# Python bytecode from the subprocess driver
drivers/__pycache__/
