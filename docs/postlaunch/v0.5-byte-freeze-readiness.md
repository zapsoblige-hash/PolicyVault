# v0.5 covenant BYTE-FREEZE readiness synthesis (stage J, 2026-09-02)

> **Outcome:** the owner authorized the freeze on 2026-09-02 over exactly these bytes — see `v0.5-covenant-byte-freeze.md` (the freeze record). This synthesis is preserved unchanged as the readiness evidence.

Prerequisites of the design freeze (`docs/postlaunch/v0.5-design-freeze.md`):
**A** token deposit builder, **B** live testnet-10 proof, **C** independent
internal reread, **D** portable core bundles. A, C, D were complete at the
2026-09-02 checkpoint; **B PASSED in this checkpoint**. This document
synthesizes the exact identities and evidence. It declares NOTHING: the
covenant bytes are frozen only by explicit owner authorization; x402 and
DEX work do not begin here.

## 1. Covenant identity

| item | identity |
|---|---|
| `contracts/PolicyVault.v0.5.sil` | sha256 **`c693aeffb59286d21d44452bde0943d78840b66cf480b629624b7747b4197dd9`** (476 lines; contract `PolicyVaultToken`; state region [1,93]; bound-2 redeem 9,273 B; static sig-ops 3) — UNCHANGED through this checkpoint |
| generator `tools/gen_v5.js` | sha256 `984df6b83dd7421df5e6a249dbf065b2f4e326ea096af25747287cebc19bd273` |
| vendored token program `contracts/vendor/kcc20-reference.sil` | sha256 `2b7d59b06c0f34461bb01ae32b642c13491dd5b90a7cb4d5b827fcebf389ef73` (byte-exact upstream KCC20; pinned by fixture + adapter) |
| frozen carriage | TRANSACTION-DERIVED VERIFIED TEMPLATE CARRIAGE (D1.1 Variant C) — the live proof exercised exactly this carriage |
| descriptor schema | `policyvault-asset-descriptor/1` (frozen; dual binding covenant-ID + hash-verified template + pinned geometry) |
| v0.5 lane | `adoption-mcp-distribution`, evidence commit **`c0454b8`** |

## 2. Live testnet-10 proof (prerequisite B) — PASSED

Node: local kaspad **2.0.1**, datadir `~/.rusty-kaspa/kaspa-testnet-10`
(selected on evidence: the August datadir with the 2026-08-23 IBD-complete
log and a graceful 2026-08-25 stop; the July location was a 7-week-staler
first IBD ending in a panic; the mainnet datadir was never touched).
Started 2026-09-02T03:56:10Z with RPC LOOPBACK-ONLY (gRPC 127.0.0.1:16210,
Borsh 127.0.0.1:17210, JSON 127.0.0.1:18210; P2P public). The node judged
the August state beyond the pruning window and performed a pruning-point
proof sync: 210,540 headers → pruning point `c1ea98ae…` → **313,899,606-
UTXO** set import → UTXO-index resync → block IBD (1,338,537 + 96,841 +
4,090 blocks). Starting DAA at first RPC probe 0 (importing); **genuinely
synced at 2026-09-02T06:45:08Z: isSynced true, hasUtxoIndex true, DAA
559,728,777, blockCount = headerCount = 1,439,715**; RSS at the time of
this synthesis ≈ 5.6 GiB. Run start DAA 559,730,490.

Test keys: the FUNDED main-repo set (owner `kaspatest:qzjvdn9j…23vhet`,
delegate/agent `kaspatest:qqxpy7rm…n22z26`, funding/user
`kaspatest:qr2smude…lgr4ld`; balances before the run 2,068.5 / 1,429.3 /
93,278.6 tKAS). No mainnet funds. Runner: `KASPA_NETWORK_ID=testnet-10
KASPA_RPC_URL=ws://127.0.0.1:18210 node tools/testnet-v5-lifecycle.js`
(first attempt failed BEFORE any broadcast on a live-path TOOL BUG — the
funding selector read the raw RPC UTXO shape although
`sdk/src/chain.js getAddressUtxos()` normalizes it; fixed in `c0454b8`;
the --dry run bypasses that helper). Second attempt: **LIVE LIFECYCLE
COMPLETE, 2026-09-02T06:48:00Z → 06:48:22Z**, evidence
`docs/testnet-v5-evidence.json`.

| step | txid / identity | chain verification (UTXO index) |
|---|---|---|
| KCC20 issuance (user-owned genesis, 2 KAS, supply 1,000,000) | **`bc37787f26be6118c3aba11adec76201080d5d39be1f6b5d75051e0ab6191232`** | output 0 at `kaspatest:prgypfr3…88tx3r`, 200,000,000 sompi, covenantId = family **`a7259d1a40d3169fbdeaebe70befb31c96f9b33a746d1bab1198cf73f73669e5`** |
| descriptor | hash **`831b7468503b5bd4d43c1f5374f99cd3c61d346ca2d6d6027b4138b1f7a2efc4`**; templateVmHash `9ed5a66c65290eea4514d36536c865a1de8413aa8d8d623f4ece346ba58163e5`; geometry prefix 1 / state 46 / suffix 1521 | corroborated against the program's real bytes |
| controller genesis (owner key) | **`d5c930ddf608e8ed4ed1394ebab7f54a3f9762ccf7da8183ffa75b62cfe7f016`**; controller covenantId **`9e08deb12f4b8536d0501e193e6ec6f32d50722438d44b92d1870014aa196499`**; stateId `8759cc542bd2e73b0165c4a9cdb60d7f24eddf7b906ab0f5ea54dfea368aca24`; scriptSha256 `e74f2ca0c3a38d0e4fbc4e788daf6d8e645bdfed698af8151f30a50ec22e9e7c` | output 0 at `kaspatest:pqp7lgk9…mf0lsv`, 100,000,000 sompi (fee reserve), covenantId 9e08deb1… |
| deposit (user signs INSIDE the family call) | **`405b1ab40b08660e6b0cdb8474d100dc5d2b708725b80e33a29908660a110a0c`** | output 0 at `kaspatest:ppfxw7eg…ggmte9`, 100,000,000 sompi, covenantId a7259d1a… (token position under the controller) |
| token agent spend (agent key; fuel from the agent's own KAS) | **`b8552d9b7383b1ccac3162e8f540baded18f2c10b4746aa06286388aa9d0077e`**; conservation 10000 → spend 2000 → 8000; successor stateId `ea654ada2c64f093aa7aa2992c35d5fe5472f8e1dc442025174f9966bbceb11c`; tokenPeriodSpent 2000 | output 0 controller continuation (100,000,000 sompi, covenantId 9e08deb1…); output 1 token continuation (80,000,000 sompi, family a7259d1a…); output 2 recipient token output (20,000,000 sompi, family a7259d1a…) — local deterministic manifest verification VERIFIED |
| negative: over-cap (3000 > cap 2500 re-encoded through the production encoder, agent-signed) | txid `f6b065bb07db4814839c35098149d3622b16d802c7d568c5d9813acd8e13d847` | **REJECTED by consensus** — "failed to verify the signature script: script ran, but verification failed" |
| negative: owner key on the agent path | same txid `f6b065bb…` (only signature-script material differs; Kaspa txids exclude signature scripts) | **REJECTED by consensus** — same refusal class |
| owner pause | **`57cc47f1c2bc17e256eb229079693abbe12c6aea76c51d9773400d7b95266fab`** | output 0 at `kaspatest:ppj50lfr…guhydy`, 100,000,000 sompi, covenantId 9e08deb1… |
| owner unpause | **`346814b351c2ed1a176e00f4f2cfe8555f7cd8a7344735e42c24256fcc1db5eb`** | output 0 at `kaspatest:pqrnvpa0…jvrnlg`, 100,000,000 sompi, covenantId 9e08deb1… |
| owner recovery | **`db1f87c929bdfb18744a1153de1ca729c68b0282763e3ae193a051f113cfa906`** | output 0: 100,000,000 sompi to the OWNER p2pk (reserve recovered); output 1: 80,000,000 sompi token family output (owner-owned), covenantId a7259d1a… |

The two negative-validation transactions are authorized testnet
negative-validation transactions constructed independently of the
PolicyVault application, verifying that consensus rejects policy-invalid
transactions even when correctly signed by the designated delegate.

**Independent post-run verification** (separate process, SDK chain helper +
`getBalancesByAddresses`): owner balance 206,852,393,000 → 206,952,393,000
sompi (exactly the recovered 1 KAS reserve); recovery outputs 0 and 1 LIVE
in the UTXO index (block DAA 559,730,746); the agent-spend recipient token
output LIVE (DAA 559,730,588); the deposit output and the spend's
controller/token continuation outputs SPENT (consumed predecessors).
CHAIN_VERIFIED was never assumed — every claim above is an observed UTXO
entry or an observed node rejection.

## 3. Portable bundle identities (prerequisite D)

`web/core-bundle.js` sha256 **`1408ce9ed03e64c103e2d1557ff4d20150e2a5c63189cec707137c1064999cf0`**
(474,426 B); `mobile/www/vendor/core-bundle.js` byte-identical; fixture
equality (engine template hash, token-agent leaf, descriptor hash) proven
through the bundle; anti-drift green.

## 4. Regression totals (v0.5 lane tip `c0454b8`; same source as the baseline runs)

| suite | result |
|---|---|
| VM (`cargo test` in tests/vm; incl. `v5_production` 37-case spend matrix, `v5_sdk_integration` production-byte vectors) | **328 / 0 fail** |
| core (`node --test core/*/test/`, incl. core/assets) | **578/578** |
| SDK (serial `npm test`) | **887: 792 pass / 0 fail / 95 env-skipped** |
| web | **322/322** |
| mobile | **82/82** |
| mcp (serial) | **45/45** |
| conformance | **20/20** |
| security/hostile-ai | **54/54** |

## 5. Measured mass / fee envelope (real engine, production pricing, bound 2)

Spend: 199,252 units incl. sig-op → budget 19 (SDK commits 28); tx
11,948 B; fee mass 23,896 g ≈ 0.0239 KAS at the 100 sompi/g default relay
floor; mempool estimator 1,268,500; sig-ops 3 (controller) / 2 (token);
redeem 9,273 B (v0.4.1: 16,980 B). Live: every transaction of §2 was
accepted at the node's default relay floor with reserve-/fuel-funded fees
as built by the SDK; the ~0.0066 KAS D1.1 figure remains an experiment
number, never a protocol assumption (fee tooling budgets from mass + the
estimator).

## 6. Known limitations (accepted-risk register, unchanged)

- `kcc20/1` adapter supports ONLY the vendored reference program family;
  other programs refuse `UNSUPPORTED_TOKEN_PROGRAM`.
- p2pk recipients only; no approval tier in the token controller.
- KCC-0001 BLAKE3 identity carried opaque (`NOT_AVAILABLE`, never inferred
  from the VM hash).
- Issuer-power corroboration depth: uncorroborated powers are surfaced
  DECLARED-ONLY.
- Fees are transient-mass-dominated; standardness: templates with family
  bound > 15 must fail the onboarding gate (static sig-op limit 15).
- The live proof exercised ONE lifecycle at family bound 2 with a single
  agent policy and a fresh issuance; rollover, depth-12/16 registries,
  owner matrix and the 37-case hostile spend matrix are VM-proven with
  production bytes, not live-repeated.
- Internal reread only (`docs/postlaunch/v0.5-covenant-reread.md`); NO
  external professional audit has occurred and none is claimed.

## 7. Divergence from the design freeze

None found. The reread (C) matched every frozen invariant against the
emitted source; the live run used the frozen carriage, the frozen
descriptor schema, dual binding, two-domain accounting (token protected
value vs KAS fee reserve) and the family-isolation guards exactly as
specified. The only defect surfaced in this checkpoint was in the live
runner tool (UTXO shape), not in the covenant, encoder, builders, or
manifests.

## 8. Verdict

Prerequisites A (deposit builder, production-byte proven), B (live
testnet-10 lifecycle, chain-verified, negatives consensus-rejected), C
(independent internal reread, no defect), D (bundles rebuilt, byte-
identical, fixture-proven) are all complete; covenant bytes unchanged at
`c693aeff…`.

**COVENANT-FREEZE-READY**

Stopped here. The covenant bytes are NOT declared frozen (owner gate);
x402 and DEX implementation are NOT started.
