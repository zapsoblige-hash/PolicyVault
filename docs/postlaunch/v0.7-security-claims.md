# v0.7 organizational M-of-N owner root — SECURITY CLAIMS (CANDIDATE, not byte-frozen)

Format: CLAIM → ENFORCEMENT → TEST → EVIDENCE, labeled PROVEN (real-engine VM
on the candidate bytes + SDK-built production bytes + live testnet-10),
PARTIALLY PROVEN, or DESIGN TARGET. All review evidence here is internal. Covenant generation v0.7 is unrelated to application release numbers.

Authority statement: AI MAY REQUEST · POLICYVAULT DETERMINISTICALLY DECIDES ·
THE COVENANT ENFORCES · SIGNERS RETAIN CUSTODY. PolicyVault is not a wallet.

| # | CLAIM | ENFORCEMENT (`PolicyVault.v0.7-root.sil` / `PolicyVault.v0.7-payment.sil`) | TEST | EVIDENCE | STATUS |
|---|---|---|---|---|---|
| 1 | Ultimate authority is an M-of-N owner set; no single key is the unilateral financial root | root `rootAction`: M-of-N over 12 in-state owner slots at ONE counting call site; the rooted vault has NO owner key and NO owner signature check — owner operations require the pinned root covenant input | `v7_root_production` (quorum rows), `v7_payment_production` (no-root / other-org / substituted-template rows), `v7_sdk_integration`, live steps 06–19 | VM + production bytes + testnet-10 (2-of-3, 3-of-3) | PROVEN |
| 2 | Duplicate owner keys are impossible; one signature never counts twice | `WF(prev)` on every root spend and `WF(new)` on ROTATE/RECOVER/SUCCESSION (pairwise distinct actives, contiguity, `1 ≤ M ≤ active`, `1 ≤ K ≤ M`, `0 ≤ R ≤ M`); each slot verified against exactly one key | rows "duplicate owner key in the rotated set / in the predecessor", "one signature replayed into a second slot"; live negative "duplicate signature across slots" | VM + testnet-10 rejection | PROVEN |
| 3 | Deterministic slot semantics; threshold mechanically covenant-enforced | fixed slot order 1..12; `count ≥ ownerM` (or `≥ emergencyK` on FREEZE only; `≥ recoveryM` on OWNER-RECOVER) | threshold sweep 1-of-1…12-of-12 with every M−1 refused; live invalid quorum M−1 | VM + testnet-10 | PROVEN |
| 4 | No stale-approval replay; no approval portability across successor states | every counted signature is SIGHASH_ALL (65-byte, `0x01` gate in-covenant) over a transaction that spends the root outpoint (single-use) and commits to the successor; `rootNonce` strictly increases | SIGHASH NONE/SINGLE/ACP rows, forged gate byte, nonce not advanced / +2, post-sign mutation; live "stale quorum vs spent root outpoint" (orphan) and "replay of accepted bytes" | VM + testnet-10 | PROVEN |
| 5 | Owner/member rotation, threshold change, succession/recovery, emergency freeze/unfreeze are covenant transitions with the stated quorums | actions 0 AUTHORIZE / 1 ROTATE / 2 FREEZE (K) / 3 UNFREEZE / 4 OWNER-RECOVER (R, idle delay, lands frozen — D6) + `rootSuccession` (successor key, idle delay, lands frozen, D1 new primary key) | honest + hostile rows per action; live ROTATE, threshold 2→3, FREEZE, UNFREEZE, OWNER-RECOVER, SUCCESSION | VM + testnet-10 | PROVEN |
| 6 | The lighter (emergency) quorum can only REDUCE authority, never expand it, and cannot become expansion by composition | FREEZE is the only K-quorum path; its only effect is `frozen 0→1`; the rooted vault accepts a FREEZE root successor ONLY for `ownerControl(4)` emergency pause | rows "FREEZE quorum used to ROTATE/UNFREEZE", "FREEZE also changing the set", "selector 0–3 under FREEZE", "selector 4 under AUTHORIZE"; live emergency pause under FREEZE K=1 | VM + testnet-10 | PROVEN |
| 7 | Authority-expanding vault changes (setAgentRoot, unpause, recover) require the full root quorum; hosted admins, delegates and approvers cannot act as owners | vault owner paths require `OpCovInputCount(orgRootCovenantId) == 1` and pin the root's EXACT successor bytes (byte-level pin: template hash + P2SH + head unchanged + tail frozen/nonce rule); no signature argument exists on owner paths | rows "no root input", "root of a different organization", "substituted root template", "frozen root", "root running ROTATE/UNFREEZE/RECOVER/SUCCESSION under a vault op"; SDK refusals `ROOT_INPUT_REQUIRED`, `OWNER_PATH_TAKES_NO_SIGNATURE` | VM + production bytes | PROVEN |
| 8 | Recovery cannot be redirected | `ownerRecover` pays the fee reserve and the whole token position to the GENESIS-PINNED `recoveryPk` | rows "recovery redirected", "partial payout", "without root authorization"; live terminal recover to the pinned cold key after three owner-set generations | VM + testnet-10 | PROVEN |
| 9 | Relative idle-delay paths (owner recovery, succession) are consensus-enforced | `this.age ≥ delay` → `OpCheckSequenceVerify` + consensus `check_sequence_lock` on the input sequence | engine rows (sequence < delay; disabled bit); **live two-phase proof with identical bytes: rejected at age 49/47, accepted at 636/624** | testnet-10 node rejection text `one of the transaction sequence locks conditions was not met` | PROVEN |
| 10 | No self-locking transition; explicit terminality | the root has no terminal action; set changes require `WF(new)` (`M ≤ active`, ≥ 1 active); owner recovery / succession are the deliberate escape hatches (opt-in by `recoveryM > 0` / `successorPk ≠ 0`) | rows "rotation to M above active / M=0 / empty set / holes", "recovery/succession when disabled" | VM | PROVEN (the "neither recovery nor succession configured and keys lost" case is a documented deliberate lock, §5 of the readiness record) |
| 11 | Exact fee/value conservation | root: successor value ≥ input − `rootMaxFeePerTx` (live: exactly preserved, fees fuel-funded); vault: v0.5 fee-reserve domain + exact successor value unchanged | rows "root value drained beyond rootMaxFeePerTx", "successor value ≠ declared fee reserve" | VM + testnet-10 (2.00000000 KAS across 11 transitions) | PROVEN |
| 12 | Every v0.5 delegate protection is retained; the delegate path never sees the root; period budgets cannot be made vacuous | payment covenant = frozen v0.5 by 13 exact edits; `tokenAgentSpend` byte-for-byte v0.5 logic + `require(periodLengthDaa > 0)` (D2) | `v7_payment_production` (v0.5-inherited rows + D2 row), live delegated spend with no root input, live over-cap 501 vs 500 rejection | VM + testnet-10 | PROVEN |
| 13 | Signer-visible intent for every root action and rooted-vault operation; refusals before signing | `policyvault-org-root-manifest/1` + `policyvault-rooted-vault-manifest/1` (parent-action vs vault-op cross-check, template pins, exact lockTime, freshness = root outpoint kill switch) + `core/explain/org-root-explain.js` + slot request/response envelopes | `org-root-manifest-v7.test.js`, `org-root-hostile-matrix-v7.test.js` (every §4 field class refused; 20/20 consensus rejects attributed), `org-root-slot-signer-v7.test.js`, `org-root-explain.test.js` | SDK (local verification) | PROVEN (local verification); the cryptographic slot↔key binding is the covenant's `checkSig` |
| 14 | Standardness / relayability | static sig-ops root 13 / vault 1 (≤ 15); standard output classes; post-Toccata block-fit | measurement tests; every live transaction accepted by the default testnet-10 node | VM + testnet-10 mempool acceptance | PROVEN |
| 15 | Economic viability | n/a (policy) | `tools/v7-fee-curve.js`, `sdk/tools/measure-v7-fee-shapes.js` (31 shapes) | `v0.7-economic-viability.md`: owner op ≈ 0.046 KAS, delegate spend +3.0 % vs v0.5, quorum size nearly free through 9 owners | REPORTED |
| 16 | Multi-vault organizations; rooted KAS (v0.4.1) and composability (v0.6) profiles | same mechanism; not built | — | — | DESIGN TARGET |

Residual risks (honest): (a) candidate bytes NOT frozen; (b) owner operations serialize on the root UTXO; (c) recovery/succession delays are DAA-based and require an operational heartbeat; (d) the root state is fixed at 12 slots; (e) template constants (successor key, delays, root fee bound) change only by root migration; (f) no app surface yet; (g) the I3C-F3 class (loose lockTime binding) was found in the frozen v0.5 manifest verifier and fixed on the integration lane — consensus was never at risk, local availability-tamper detection was.

## ADDENDUM — v0.7-kas ROOTED KAS SAFE-PAYMENT PROFILE (Wave 2, Track C, 2026-09-03)

Row 16 above ("rooted KAS (v0.4.1) profile; same mechanism; not built;
DESIGN TARGET") is now PARTIALLY superseded: the rooted KAS profile is
BUILT, VM-VERIFIED, SDK-INTEGRATED (production-byte proven) and
core-boundary hostile-matrix proven. This addendum is additive — row 16
is left unedited above (composability/v0.6-rooted remains a design
target; only the KAS/v0.4.1-rooted half of that row has progressed) —
full detail in `docs/postlaunch/v0.7-kas-profile-readiness.md`.

**UPDATE (Wave 2, Track C2, 2026-09-03, later same day):** the two
"documented inherited behavior" rows K9(1)/K9(2) below were CANDIDATE
HARDENED (not merely documented) — a root-covenant rider on the
delegate path and a non-SIG_HASH_ALL agent signature are now REFUSED,
not accepted. See K9' and K11 below, which supersede K9 for the current
candidate bytes (K9's original text is left unedited above as the
historical record of what was true before this hardening). K10 below
is superseded by K10': the SDK layer, the core-boundary hostile matrix
and a --dry-run-proven testnet-10 lifecycle tool now exist.

| # | CLAIM | ENFORCEMENT (`PolicyVault.v0.7-kas.sil`) | TEST | EVIDENCE | STATUS |
|---|---|---|---|---|---|
| K1 | Ultimate authority over the KAS vault is the SAME organizational root as the payment profile; no owner key exists | `requireRootAuthorization` + `requireOnlyRootCovenantInputs`, copied verbatim from `PolicyVault.v0.7-payment.sil`; `pubkey owner` removed | `v7kas_hostile_matrix_all_refused` (no-root, other-org, substituted-template, frozen-root, two-roots, alien-rider rows), `v7kas_under_quorum_is_refused_by_the_root_input` | VM (real TxScriptEngine, real Schnorr) | PROVEN (VM level; NOT yet testnet-verified) |
| K2 | `ownerControl` selectors 0-5 require the FULL root quorum (AUTHORIZE); selector 6 (EMERGENCY pause) is the ONLY vault effect reachable from the LIGHTER emergency quorum (FREEZE), and is AUTHORITY-REDUCING/monotone | one inline `requireRootAuthorization(expectFrozenPush)` call site, `expectFrozenPush` computed from `opSelector == 6` | rows "selector 0-5 pinned to a FREEZE root successor" (6 rows, all refused), "EMERGENCY pause pinned to AUTHORIZE" (refused), "EMERGENCY pause on an already-paused vault" / "silently changing the agent root" (refused); honest `v7kas_honest_owner_control_selectors_0_to_6` | VM | PROVEN |
| K3 | Recovery cannot be redirected | `ownerRecover` pays `protectedValue + feeReserve` to the GENESIS-PINNED `recoveryPk`, under the root's FULL quorum | rows "recovery redirected to an attacker key", "recovery without the root's authorization", "recovery under a FREEZE root successor" (all refused); honest `v7kas_honest_owner_recover_to_the_genesis_pinned_destination` | VM | PROVEN |
| K4 | Every v0.4.1 delegate protection is retained, byte-for-byte, in the new generation | `agentSpend` unchanged except D2; frozen v0.4.1 never modified (delta generator fails closed on drift) | named-category delegate hostile tests: over-cap, over-budget, premature rollover, non-member recipient, forged proof, approval shortfall, duplicate approver, non-approver signature, SIGHASH variants, post-sign mutation, fee-domain abuse, value leakage (all refused); D2 row refused | VM | PROVEN |
| K5 | D2 (`periodLengthDaa > 0`) closes the same vacuous-period gap already closed in the payment profile | `require(periodLengthDaa > 0)` at the top of `requireAgentTransition` | `v7kas_d2_zero_length_period_rejected` | VM | PROVEN |
| K6 | Standardness / relayability | static sig-ops 11 (≤ 15, real scanner) | `v7kas_measurement_standardness_and_stack` asserts `real_sigops == 11` | VM (`post_toccata_p2sh_sig_scanner`) | PROVEN |
| K7 | The call-encoder byte path (not just the in-process library encoder) produces correct, VM-accepted bytes for every v0.7-kas entrypoint, and correctly refuses malformed calls before they ever reach the VM | `pv_call_encoder`'s new `policyvault-0.7-kas` arm (opSelector bounds 0..6, no-signature-on-owner-paths) | `v7_kas_encoder_integration.rs`: honest agentSpend via the binary, 3-row mutation matrix (all refused), opSelector-out-of-range refused by the encoder itself, a signed owner call refused by the encoder itself | VM + the compiled production binary | PROVEN |
| K8 | Economic viability | n/a (policy) | real-engine measurement at MINIMAL required budgets | `docs/postlaunch/v0.7-kas-economic-viability.md`: owner op ≈ 0.064 KAS, delegate spend ≈ 0.040 KAS (both higher than the payment profile, explained by the vault-level approver machinery this profile carries and the payment profile does not) | REPORTED |
| K9 | Two measured, investigated, classified behavioral differences from the payment profile are SAFE, not defects | (1) `agentSpend` accepts a root-covenant rider (v0.4.1 never had a foreign-covenant check; output binding is structural, not signature-dependent); (2) a non-ALL agent signature is accepted (same structural-binding reason; only the 10 approval slots carry an explicit A7 sighash gate) | `v7kas_delegate_spend_accepts_a_root_covenant_rider_documented_inherited_behavior`, `v7kasenc_non_all_agent_sighash_is_accepted_documented_inherited_behavior` | VM | SUPERSEDED — see K9' below (kept verbatim as the historical pre-hardening record) |
| K9' | K9's two rows are now CANDIDATE-HARDENED, not merely accepted-and-documented: `agentSpend` REFUSES a root-covenant (or any other) rider on the delegate path, and REFUSES a non-SIG_HASH_ALL / forged-gate-byte agent signature, in exact parity with the already-proven gate `contracts/PolicyVault.v0.6.sil` uses (`requireAgentAuthorization`) | `tools/gen_v7_kas.js` edit E6: `requireAgentAuthorization(byte[] agentSig, pubkey agentPk)` (65-byte length + trailing-0x01 gate before `checkSig`) and `requireOnlyPlainInputsOnDelegatePath` (every non-vault input on `agentSpend` must be plain) | `v7kas_delegate_spend_refuses_a_root_covenant_rider` + positive control; `v7kas_delegate_spend_refuses_non_all_agent_sighash`; `v7kas_delegate_spend_refuses_a_forged_gate_byte` + positive control (all in `v7_kas_production.rs`, 34/34 passed); `v7kasenc_non_all_agent_sighash_is_refused`, `v7kasenc_forged_agent_sig_gate_byte_is_refused` + positive control (`v7_kas_encoder_integration.rs`, 7/7 passed) | VM + production-byte (compiled `pv_call_encoder` binary) | PROVEN. New covenant sha256 `393f2130b48d06ba4d5b29ad8846098dd16ae89ebfd658a2df163f0801ac1d8b`; generator sha256 `1cd3d30be883afb338ac156c4bb9c983b198e954e27c4e0ef1a5769a161a7014`. Static sig-ops unchanged (11); MAX_STACK_SIZE headroom 78 (was 84) |
| K10 | SDK-level signer-visible intent (manifest), core-boundary hostile matrix, and a live testnet-10 lifecycle | NOT BUILT this session | — | — | SUPERSEDED — see K10' below |
| K10' | The SDK transaction-building layer, the signer-visible manifest family, router registration, cross-runtime portability, the core-boundary hostile matrix, and a testnet-10 lifecycle tool now exist and are DRY-RUN PROVEN | `sdk/src/contract-compiler-v7-kas.js`, `sdk/src/vault-builders-v7-kas.js`, `core/intent/org-root-manifest-v7-kas.js` (`policyvault-org-root-kas-manifest/1` + `policyvault-rooted-kas-vault-manifest/1`), `core/intent/router.js` registration, `tools/testnet-v7-kas-lifecycle.js` | `sdk/tools/gen-v7-kas-vectors.js` -> `tests/vm/tests/v7_kas_sdk_integration.rs` (5/5 passed: 16 accept / 12 reject SDK-built vectors, 15 SDK refusals, 9 manifest checks); `core/crossruntime/test/v7-kas-portability.test.js` (23/23 passed); `sdk/test/rooted-kas-hostile-matrix.test.js` (9 field-class rows: amounts, recipient, approvals, root pins, template pins, lockTime binding, parent-action cross-check — all refused, 2/2 tests passed); `tools/testnet-v7-kas-lifecycle.js --dry-run` (44 steps, 5/6 negatives — 11f-replay is legitimately skipped in synthetic dry-run mode, matching the payment profile's own lifecycle tool) | SDK/production-byte + cross-runtime + core-boundary hostile matrix + DRY-RUN | PROVEN at every layer EXCEPT live chain. **NOT testnet-verified on a live node** — a local testnet-10 node is confirmed running and reachable, but the worktree's freshly-generated test funding key has zero balance and no faucet was available this session; live execution needs owner-provided testnet KAS or a faucet before the LIVE (non-dry-run) path can run |

Residual risks specific to this profile (honest, additive to the risks
above, UPDATED 2026-09-03 later the same day): (a) candidate bytes NOT
frozen, NOT LIVE-testnet-verified (dry-run
proven only — see K10'); (b) the SDK transaction-building layer NOW
EXISTS and is production-byte proven, but has never moved a real
transaction on a live node; (c) the vault redeem (18,941 B, up from
18,651 B after the E6 hardening) is roughly double the payment
profile's (9,636 B) because of the retained vault-level M-of-N
approver machinery, which costs redeem-size but not execution cost
when unused; (d) the `requireRootAuthorization` /
`requireOnlyRootCovenantInputs` text is copied verbatim from the
payment profile, not independently re-derived — a future defect found
there via the payment profile's own review applies here too and must
be re-checked; (e) `requireAgentAuthorization` /
`requireOnlyPlainInputsOnDelegatePath` (E6) are NEW code for this
profile, not shared with the payment profile — they mirror v0.6's
already-proven `requireAgentAuthorization` pattern but are not
byte-identical to it and have not been independently adversarially
reviewed beyond this session's own hostile rows; (f) the explain
(signer-readable rendering) layer for this profile was NOT built this
session — `core/explain/org-root-explain.js` covers only the payment
profile; a signer today must read the raw manifest JSON or wait for a
future explain extension.

> **Evidence note (2026-09-09, [CLAUDE], rc29 continuation):** the SDK production-byte suites cited for the v0.7 payment and KAS profiles (`tests/vm/tests/v7_sdk_integration.rs`, `v7_kas_sdk_integration.rs`) were RED between the R7-02 correction (`9da991f`, 2026-09-07) and 2026-09-09 because their vector generators predated the full-policy-set and predecessor-redeem contracts; the generators were corrected (no builder, verifier or covenant byte changed) and the suites re-executed on the real engine: 4/4 and 5/5, alongside `v7_payment_production` 18/18 and `v7_root_production` 17/17 (`docs/postlaunch/v0.7-covenant-byte-freeze.md`, post-freeze note). No claim in the table above changed; this note records the evidence gap and its closure.
