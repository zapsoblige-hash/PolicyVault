# v0.6 ATOMIC COMPOSABILITY — BYTE-FREEZE READINESS RECORD (stop packet, 2026-09-03)

**Verdict: V0.6-ATOMIC-COMPOSABILITY-TESTNET-VERIFIED-BYTE-FREEZE-READY.**

Meaning, exactly: the candidate covenant `contracts/PolicyVault.v0.6.sil`
is VM-VERIFIED, PRODUCTION-BYTE-PROVEN and TESTNET-VERIFIED (testnet-10,
test assets, fixture venue) for BOTH swap directions, with the hostile
matrix, standardness/relayability, economic viability and the owner's
evidence addendum (A–D) recorded. It is **NOT byte-frozen** (that is a
subsequent owner decision), NOT production, NOT a
mainnet swap, NOT an endorsement of any venue. Covenant generation v0.6 is
unrelated to application release v1.6.0. Frozen v0.5 is untouched.

## Identities

| item | value |
|---|---|
| candidate covenant `contracts/PolicyVault.v0.6.sil` | sha256 `c7c5f22c54a55d933ec8440a28bc2c628b9b99262541d50dbe9ffbdd16ba025c` (generator `tools/gen_v6.js`; contract `PolicyVaultV6`; redeem 24,920 B at the reference template; state region [1,135]; 5 static sig-ops) |
| frozen v0.5 (re-verified before and after; UNCHANGED) | `c693aeffb59286d21d44452bde0943d78840b66cf480b629624b7747b4197dd9` |
| pool FIXTURE `contracts/experiments/V6PoolFixture.sil` (not a product) | sha256 `c3590c82d81679bd00b3a8342604ffd220bf557fc72d937e84832a12c96a6faa` |
| architecture freeze record | `docs/postlaunch/v0.6-architecture-freeze.md` |
| leaf domains | agent `0x50563601` (149 B), swap policy `0x50563602` (229 B); fixtures `core/model/test/fixtures/{token-agent-leaf-v6,swap-policy-leaf-v6}.json` pinned to the Rust leaf functions |
| encoder arms (`tests/vm/src/bin/pv_call_encoder.rs`) | `policyvault-0.6`, `v6-pool-fixture/1`, kcc20/1 delegate |
| manifest | `policyvault-swap-intent-manifest/1` (`core/intent/swap-manifest-v6.js`, 51 checks) |
| venue profile | `policyvault-swap-venue-profile/1` (`core/model/swap-policy-v6.js`) |

## Gates (all green)

| gate | result |
|---|---|
| VM production suite `tests/vm/tests/v6_production.rs` (real TxScriptEngine, real Schnorr) | **42 passed / 0 failed**: honest SELL A, SELL B, BUY, pool-note-first, production depths (agent 12 / swap 12), rollover, spend, owner 0..5, recover; hostile: unapproved pool family, alien pool template, pair substitution, token cap/budget, KAS cap/budget/no-authority, min-out/floor, max-in/ceiling, direction mask, type-B buy, type-A extra output, type-B redirected/inexact proceeds, hidden output, hidden covenant input, **external plain KAS input**, conservation ±1, note KAS leak, malformed successor, reserve over cap / above exact fee, principal tamper/exhaustion, agent/swapRoot/nonce/paused mutation, paused, forged/stale swap leaf, forged agent leaf, protocol fee over max / wrong key, SIGHASH SINGLE|ANYONECANPAY / NONE / post-sign mutation, wrong signer, pool invariant break (pool refuses), v0.5-shaped spend with pool present, owner riders/cross-selector, recover partial payout |
| measurement + standardness (`v6_measurement_units_mass_standardness`) | sell/buy tx 40,133–40,809 B; compute 46,773–47,849; transient 160,532–163,236; fee mass 80,266–81,618 g; controller priced units 385,481 (depth 1) / 426,614 (depth 12+12) → budgets 38/42 (SDK model 45); pool 90k → 9 (SDK 23); static sig-ops 5 ≤ 15; every output class standard; post-Toccata block-fit limits respected (covenants exist only post-Toccata: the pre-Toccata 100k cap cannot apply) |
| core unit tests | 117/117 (`core/model/test`, `core/intent/test`), purity/api-surface guards updated |
| production-byte integration `tests/vm/tests/v6_sdk_integration.rs` | **36 SDK-built vectors: 18 accept (every input executes within the SDK's committed budgets) / 18 reject (consensus refuses; incl. external KAS input, redirected proceeds/fee, principal tamper, consideration mismatch, wrong signer, pool invariant break) + 25 SDK pre-build refusals** |
| swap manifest / verifier | `sdk/test/swap-manifest-v6.test.js`: SELL A/B + BUY VERIFIED; 11 tamper classes REFUSED; deadline refusal |
| stablecoin readiness | `sdk/test/v6-stablecoin-readiness.test.js` 2/2 (no hard-coded stablecoin, issuer powers verbatim, unknown standard fails closed) |
| SDK full suite / full VM suite (regression incl. v0.5 encoder arms) | see the continuation notes for the final counts recorded at checkpoint |

## LIVE TESTNET-10 PROOF (`docs/testnet-v6-atomic-evidence.json`, run 2 = definitive; node v2.0.1, synced, utxoindex, DAA 560,487,906 at connect)

| step | txid | chain facts |
|---|---|---|
| KCC20 issuance (user note) | `5d6141a814d7d7eacb356a02a611295e029891ea255fd3714c712443e97648e4` | family `6fae08bf884dc61a6bee27fdaba78dc090330e19ca179383fb89bd883944dbfa` |
| pool fixture genesis (500 KAS reserve) | `82e6220fe476f11135306f3f8749aa2eb51852a913f0ab872ae6af91cf630676` | pool id `465dead11468d994a9b99a1700f9bea11e115f3468d6112bcd8dcd55a0bca13c`; venue profile hash `651ae43065399bc80e66f772424c5ff1624adff2216a1dc9e4d61237027a9a23` |
| pool reserve note (100,000 tokens → pool id) | `77faa9e8f5c88ec3987b8bec9b0ad00255bbf9f2a47f2b32b80c7f8d46d097cb` | |
| v0.6 controller genesis (2 KAS reserve + 100 KAS principal) | `3967303a8a8a3de03d974647b78672a9e0c037f57f21baec80c8e688361a3756` | controller id `9c4f65a41cce16ca66ebcea7d5dc6789e7f43308f1da714335aa422a6f94c06e`; state id `e7e5453883c064ba…`; script sha256 `fc7d7372b8aac30d…` |
| deposit (10,000 tokens → controller) | `bcbecbeb4ac92a7bccd571d62fd325480e6e7880dc281d6dc126bb1319914734` | |
| **ATOMIC SELL type A** (2,000 tokens) | **`edc0d8eb7d485f0d537298d0dd071d4d7771c0ef48fa9ddb72e11a81538c7705`** | AUTHORIZED (manifest `09505021b74f3dee…`, 51 checks) → SIGNED (DAA 560,488,108; deadline 560,490,026) → BROADCAST → CHAIN_VERIFIED (5 outputs exact; pool `82e6220f…:0` + controller `3967303a…:0` consumed) → VERIFIED_OUTCOME: position 10,000→8,000; pool note 100,000→102,000; **principal 100.000→109.57958310 KAS (+9.57958310)**; pool KAS 500→490.22491520; protocol fee 0.19550170; network fee 0.08018200 from the reserve (2.0→1.919818); fee = 84 bps of swap value; storage mass 70,995 |
| **ATOMIC BUY** (1,000 tokens) | **`b5e78e23500807d593d8562780778e9999e8395681ea1d6a6ab34e54f527105e`** | AUTHORIZED (manifest `5aaa68fe5eef0f44…`) → SIGNED (DAA 560,488,245; deadline 560,490,150) → BROADCAST → CHAIN_VERIFIED (pool `edc0d8eb…:3` + controller `edc0d8eb…:0` consumed) → VERIFIED_OUTCOME: position 8,000→9,000; pool note 102,000→101,000; **principal 109.57958310→104.61389976 KAS (−4.96568334 = pool 4.86831700 + protocol fee 0.09736634)**; pool KAS 490.22491520→495.09323220; network fee 0.08018800 from the reserve; fee = 162 bps of swap value; storage mass 122,553 |
| negative: distinct correctly-signed swap against the PRE-swap pool + controller outpoints | `5d709c4b87b9690b6b8d8a4656ddb29c33ff2e2ce5b57ac887a6b1a70dede4a1` | REJECTED by the node ("is an orphan where orphan is disallowed": both referenced outpoints were spent — the transaction-level kill switches of addendum B) |
| negative: accepted SELL bytes resubmitted | `edc0d8eb…` | REJECTED ("was already accepted") |
| negative: over-cap sell re-encoded through the real encoder, delegate-signed (5,001 > cap 5,000) | `4bc6881af393bc5d1cb646ca18c6e583e4285458fe604e51371dbf44b77bb8f5` | REJECTED ("script ran, but verification failed") |
| negative: owner key signs tokenAtomicSell | `4bc6881a…` (same bytes, different signer) | REJECTED |
| negative: protocol-fee output redirected to the agent key | `1c8aaf08299b9304837f3f259e2a9e9f94a90875da9211fd0765c513a6823606` | REJECTED |
| negative: pre-sign deadline passed | local | finalizer refuses `DEADLINE_PASSED` (core boundary, not consensus) |
| owner setSwapRoot (selector 4) | `674005abff9debe82be9f24e93bbcf7948a12c4c1a409ecbe13edabcbacef989` | CHAIN_VERIFIED (nonce +1; value preserved) |
| owner recover (reserve + principal + tokens → owner) | `371fa5f51313081c6c64ee58e2ece3697dc6e1d01a24acf8ec2d06c79590fa1d` | CHAIN_VERIFIED: 106.45352976 KAS to the owner key; 9,000-token note to the owner key |

An earlier full run (run 1: sell `536649fb966d70b4…`, buy `4f82ecdad012b30f…`,
controller `0cdab6b9fc1f2e26…`, pool `c2a3d57d6382c423…`) produced identical
economics; its only weaker case (the accepted bytes resubmitted) was
superseded by run 2's distinct stale swap. Every live transaction was
signed by the intended test key through the SDK finalizer (SIGHASH_ALL),
broadcast through the default local node, and reconciled from the bytes
PolicyVault built — never from an indexer.

## Addendum (2026-09-03) coverage

- **A economic viability:** `docs/postlaunch/v0.6-economic-viability.md` —
  relay-floor fee ≈ 0.0816 KAS flat; 8.2 % of a 1 KAS swap, 1.6 % at 5,
  0.82 % at 10, 0.16 % at 50, 0.08 % at 100, 0.016 % at 500, 0.008 % at
  1,000 KAS; KIP-9 storage mass makes bps-sized protocol-fee outputs
  INVALID below ~50 KAS unless padded (effective 10.5 % at 1 KAS, 1.9 % at
  5, 0.87 % at 10). Classification: ECONOMICALLY NOT VIABLE below ~10 KAS,
  marginal 10–50 KAS, viable from ~50 KAS at the relay floor. The live
  swaps (9.58 / 4.97 KAS) cost 84 / 162 bps — consistent with the curve.
- **B freshness residual:** both kill switches recorded in the manifest
  `freshness` section, the explanation, the security claims and PROVEN live
  (distinct stale swap rejected); the pre-sign deadline is enforced by the
  core/finalizer; the residual (signed transaction consensus-valid while both
  outpoints remain unspent) is stated verbatim; workflow verify → sign →
  broadcast immediately is what the live tool does; no signed swap is ever
  stored.
- **C external BUY fuel:** NOT admitted by this candidate — `tx.inputs.length
  == 4` is pinned in-covenant; mechanically proven by
  `v6_rejects_external_plain_kas_input_on_swaps` (VM, sell/buy/type-B) and
  vector `neg_swap_external_kas_input` (production bytes). Any future
  external-funding design reopens with the addendum's eight falsification
  tests.
- **D:** Option A retirement is closed history (2026-09-02); not carried as
  an open task.

## Limitations and unresolved risks (honest)

1. Bytes not frozen;; no mainnet swap performed.
2. Venue support = the PolicyVault pool FIXTURE profile only. KaspaKaha /
   KRON are NOT supported, NOT blessed; a real venue needs its own reviewed
   `policyvault-swap-venue-profile/1` and reopened VM + production-byte +
   testnet proofs (state layout, fee arithmetic, template identity).
3. BUY uses the merge model (our note + pool note → our note + pool note):
   a controller must already hold a position (deposit ≥ 1 unit first).
4. Principal exits only via SELL outcomes or `ownerRecover`; there is no
   owner principal-withdraw selector.
5. Controller redeem 24,920 B → ≈ 0.08 KAS flat network fee; small swaps
   are not economically viable (see A). A single-entrypoint variant could
   cut ~6 KB but was not chosen (clarity of failure modes).
6. `deadlineDaa` is a pre-sign boundary only (Kaspa lockTime has no upper
   bound).
7. Token-note carries and the protocol-fee output must be sized for KIP-9
   storage mass by the builder (guarded; refusal `STORAGE_MASS_OVER_LIMIT`).
8. v0.6-labelled manifests exist for swaps only; spend/owner/recover on a
   v0.6 controller are verified by the builder's frozen bytes (the v0.5
   token manifest is version-pinned to v0.5) — an application-integration
   gap, not a covenant gap.
9. MCP usage telemetry: untouched (durable TODO).

## What this record does NOT authorize

No byte freeze, no production DEX, no mainnet swap, no production deploy,
no DNS, no push, no recurring spend, no blessing of any venue. Byte-freezing
v0.6 requires a subsequent owner decision.

## Corrections appended by the 2026-09-03 adversarial review (record kept intact above)

- Limitation 4 direction is reversed above: the protected principal DECREASES
  only via BUY (`prev − kasSpend`) or `ownerRecover`; type-A SELL strictly
  increases it, type-B preserves it.
- `v0.6-atomic-composability-design.md` (sha-pinned by the architecture freeze,
  therefore not edited) states the floor as `minKasOut·floorDen ≥
  amountIn·floorNum`; the covenant enforces the STRONGER realised-amount form
  `netProceeds·sellFloorDen ≥ amountIn·sellFloorNum`.
- Period budgets are covenant-enforced only when `periodLengthDaa > 0`
  (no in-covenant check; owner-committed leaves only; shared-core
  normalizers refuse zero — pinned by
  `core/model/test/period-length-positive-invariant.test.js`).
- Fee-reserve consumption is bounded per transaction only (no per-period cap);
  output-set pinning is swap-scoped; `ownerRecover` without the token input
  strands the position (SDK always includes it).
- Undocumented but enforced (fail-closed): `minKasOut > 0` and `maxKasIn > 0`
  are mandatory; `periodsElapsed ≤ 1000`; SELL requires `directionMask % 2 ==
  1`; a type-A leaf's `destIdentity` must be all-zero; the pool's continuation
  OUTPUT is not template-verified by the controller (only its input redeem is —
  the venue self-enforces); `tx.time` compiles to OP_CHECKLOCKTIMEVERIFY
  (controller input `sequence != MAX`, lockTime domain must match); leaf
  integers are `OpNum2Bin` sign-magnitude (negative leaf values fail closed).
The verdict of this record is unchanged; the byte freeze is recorded in
`v0.6-covenant-byte-freeze.md`.
