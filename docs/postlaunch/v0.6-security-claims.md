# v0.6 atomic-composability controller — SECURITY CLAIMS (COVENANT-BYTE-FROZEN 2026-09-03; see v0.6-covenant-byte-freeze.md)

> **Adversarial-review corrections (2026-09-03, independent reviewer + coordinator re-verification; freeze record §3):** row 4 is SPLIT — per-transaction caps are PROVEN unconditionally; PERIOD budgets are PROVEN **conditional on `periodLengthDaa > 0`**, which the covenant does NOT check (inherited from v0.3–v0.5; a zero-length period makes the period budget vacuous; only an OWNER-committed leaf can carry it; the shared-core normalizers refuse it — pinned by `core/model/test/period-length-positive-invariant.test.js`). Row 8: the reserve bound is PER TRANSACTION (no per-period cap; reserve can only ever become network fee — never stolen — at most `agentMaxFeePerTx` per transaction). Row 9: output-set pinning (`tx.outputs.length == 5/6`) is SWAP-SCOPED; spend/owner/recover bound outputs via `txFee`'s ≤ 8 and the exact successor/value rules (an attacker-funded extra plain output on a spend is accepted only while it consumes NO vault value — proven). Residual (d) is CORRECTED below. New residual (g): `ownerRecover` with no token input in the transaction terminates the controller id and permanently strands any KCC20 note still owned by it — the SDK must always include the position.

Format: CLAIM → ENFORCEMENT → TEST → EVIDENCE, labeled PROVEN (real-engine
VM + production bytes + live testnet-10), PARTIALLY PROVEN, or DESIGN TARGET.
All review evidence here is internal. Covenant generation v0.6 is
unrelated to application release v1.6.0. PolicyVault is not a DEX: the pool
in every test is a FIXTURE venue, never a product or an endorsement.

Authority statement: AI MAY REQUEST · POLICYVAULT DETERMINISTICALLY DECIDES ·
THE COVENANT ENFORCES · SIGNERS RETAIN CUSTODY.

| # | CLAIM | ENFORCEMENT (contracts/PolicyVault.v0.6.sil) | TEST | EVIDENCE | STATUS |
|---|---|---|---|---|---|
| 1 | Every v0.5 token protection is retained (dual binding A/B, template carriage, exact conservation, fee-reserve domain, owner control, recovery) | `tokenAgentSpend` / `ownerControl` / `ownerRecover` keep the v0.5 rules; `requireNoForeignCovenantInputs` still forbids any foreign covenant on those paths | `tests/vm/tests/v6_production.rs` (spend/owner/recover honest + hostile), `v6_sdk_integration.rs` | 42/42 VM; 36 production-byte vectors; live deposit/spend-shape ops | PROVEN |
| 2 | An atomic swap composes with ONE owner-approved pool family AND template only | `requireSwapPolicy` (leaf under `swapRoot`) + `requireSwapShape` (`OpCovInputCount(pool)==1`, `readInputStateWithTemplate` on the pool redeem) + `requireOnlyApprovedCovenantInputs` | `v6_rejects_wrong_venue_unapproved_pool_family`, `v6_rejects_wrong_pool_template_version`, `v6_rejects_forged_or_stale_swap_policy`; vector `neg_swap_pool_family_swapped` | VM + production bytes | PROVEN |
| 3 | Exact pair binding: the pool trades the controller's pinned token family only | the pool's reserve note must be a `tokenCovenantId` family input owned by the pool id (`requireTokenNotes`) | `v6_rejects_wrong_input_asset_family_pair_substitution` | VM | PROVEN |
| 4 | Give up no more than authorized: token cap/budget (SELL), KAS cap/budget (BUY) | `requireAgentTransition` (149-byte leaf; both counters, one period clock) | `v6_rejects_sell_over_token_cap_and_budget`, `v6_rejects_buy_over_kas_cap_and_budget`; SDK refusals; live `negative-over-cap-sell` | VM + SDK + testnet-10 node rejection | PROVEN |
| 5 | Receive no less than required: min-out / floor (SELL), max-in / ceiling (BUY) | `netProceeds >= minKasOut`, `netProceeds*floorDen >= amountIn*floorNum`; `kasSpend <= maxKasIn`, `kasSpend*ceilDen <= tokensOut*ceilNum` | `v6_rejects_sell_proceeds_below_min_or_floor`, `v6_rejects_buy_above_max_kas_in_or_ceiling` | VM (+ SDK pre-build refusals) | PROVEN |
| 6 | BUY consideration comes ONLY from the protected swap principal, accounted per agent | `newState.swapPrincipal == prev − kasSpend`; successor value = feeReserve + swapPrincipal; `kasSpend <= kasMaxPerSwap`, period budget | `v6_rejects_principal_tamper`, `v6_rejects_buy_principal_exhaustion`; vectors `neg_buy_*` | VM + production bytes + live BUY (principal −4.966 KAS exactly) | PROVEN |
| 7 | SELL proceeds go to the approved destination only (A: principal; B: allowlisted P2PK, exact value) | type A: no proceeds output, principal += net; type B: `OpOutputCovenantId==0`, `P2PK(destIdentity)`, `value == netProceeds` | `v6_rejects_type_b_redirected_or_inexact_proceeds`, `v6_rejects_type_a_sell_with_a_proceeds_output`, `v6_rejects_type_b_destination_on_buy` | VM + vectors `neg_swap_proceeds_*` | PROVEN |
| 8 | Bounded protocol fee to the venue's pinned key; bounded network fee from the fee reserve only | `protocolFeeValue` (≤ `maxProtocolFeeKas`, P2PK(poolFeePk)); `requireFeeReserveDomain` (consumed ≤ agentMaxFeePerTx ≤ exact fee) | `v6_rejects_protocol_fee_above_bound_or_to_wrong_key`, `v6_rejects_reserve_consumption_above_agent_cap`, `..._above_the_exact_fee`; live `negative-protocol-fee-redirected` | VM + testnet-10 rejection | PROVEN |
| 9 | No hidden inputs/outputs; no external KAS input rides a swap | `tx.inputs.length == 4`; `tx.outputs.length == 5/6`; every input a pinned family or plain — and plain is impossible with count 4 | `v6_rejects_external_plain_kas_input_on_swaps`, `v6_rejects_hidden_extra_output`, `v6_rejects_hidden_covenant_input`; vectors `neg_swap_external_kas_input`, `neg_swap_hidden_extra_output` | VM + production bytes | PROVEN (addendum C satisfied by construction: no external fuel exists) |
| 10 | Exact token conservation and exact KAS separation (token-note carries never fee/consideration) | `selfNew.amount == ours ∓ delta`, `poolNoteNew.amount == pool ± delta`; `selfOut.value >= ourIn.value` + exact fee equation | `v6_rejects_token_conservation_break`, `v6_rejects_kas_leak_from_our_token_note`, `v6_rejects_reserve_consumption_above_the_exact_fee` | VM + vectors | PROVEN |
| 11 | Successor binding and unauthorized state mutation refused | `requireVaultGlobalPreserved` (paused/nonce/swapRoot), agentRoot = fold of advanced leaf, exact successor value | `v6_rejects_unauthorized_state_mutation_in_successor`, `v6_rejects_malformed_successor_value`, `v6_rejects_forged_agent_policy` | VM | PROVEN |
| 12 | SIGHASH_ALL only; post-sign mutation invalid | `requireAgentAuthorization` (65-byte sig, hash byte 0x01) on spend/sell/buy | `v6_rejects_non_all_sighash_and_post_sign_mutation`, `v6_rejects_wrong_signer`; live `negative-wrong-signer` | VM + testnet-10 rejection | PROVEN |
| 13 | Exact pool-outpoint binding + controller-outpoint binding = two transaction-level stale-swap kill switches | consensus: a transaction referencing a spent outpoint is invalid; the build binds both outpoints | live `negative-stale-swap-spent-outpoints` (a distinct correctly signed swap against the pre-swap outpoints is rejected) | testnet-10 node rejection; manifest `freshness` section | PROVEN (kill switches); the pre-sign `deadlineDaa` is enforced by the core/finalizer only — DESIGN BOUNDARY, not consensus expiry |
| 14 | Deterministic pre-sign verification recomputes every financial fact from bytes; refuses tampering | `core/intent/swap-manifest-v6.js` (51 checks: quote recomputed from revealed pool state, redeems, continuation reconstruction, deltas, policies, freshness, storage mass, economics) | `sdk/test/swap-manifest-v6.test.js` (11 tamper classes REFUSED) | SDK | PROVEN (local verification) |
| 15 | Deterministic explanation and honest freshness residual | manifest `explanation` (10 fixed lines) + `freshness.residual` | same test; live evidence carries every explanation | SDK + testnet-10 evidence | PROVEN |
| 16 | Post-sign identity verification (txid recomputed from consensus bytes) | frozen-tx-v3 / pv_tx_probe (unchanged from v0.5) | live tool asserts node txid == planned txid on every broadcast | testnet-10 | PROVEN |
| 17 | Standardness / relayability | static sig-ops ≤ 15 (5 in the redeem), standard output classes, post-Toccata block-fit limits (compute ≤ 500k, transient ≤ 1M, storage ≤ 500k) | `v6_measurement_units_mass_standardness`; builder storage-mass guard; default-node broadcast of both swaps | VM + testnet-10 mempool acceptance | PROVEN for the fixture shape; KIP-9 storage mass makes small swaps INVALID unless outputs are padded (see economic viability) |
| 18 | Economic viability | n/a (policy) | `tools/v6-fee-curve.js` | `docs/postlaunch/v0.6-economic-viability.md`: ~0.08 KAS flat fee; not viable < ~10 KAS, marginal 10–50, viable ≥ 50 KAS at the relay floor | REPORTED (not a covenant property) |
| 19 | Stablecoin readiness (no hard-coded stablecoin, issuer powers surfaced, exact template pinned) | descriptor architecture (unchanged) | `sdk/test/v6-stablecoin-readiness.test.js` | SDK | PROVEN (architecture/conformance evidence only) |
| 20 | KaspaKaha / KRON / any real venue supported | — | — | NOT CLAIMED: the venue profile describes only the PolicyVault pool FIXTURE; a real venue needs its own reviewed profile (template hash, state layout, fee model) and reopened proofs | DESIGN TARGET |

Residual risks (honest): (a) the candidate bytes are NOT frozen; (b) `deadlineDaa` is not a consensus expiry (residual
documented everywhere); (c) BUY requires an existing controller position
(merge model) — a first BUY needs a prior deposit of ≥ 1 atomic unit;
(d) [CORRECTED 2026-09-03] the protected principal DECREASES only via BUY
(`prev − kasSpend`) or `ownerRecover`; a type-A SELL strictly INCREASES it and a
type-B SELL preserves it; there is no owner principal-withdraw selector;
(e) the controller redeem is 24,920 B — fees are flat ≈ 0.08 KAS at the
relay floor; (f) the constant-product fee arithmetic is the fixture's — a
real venue's arithmetic must be pinned by its own profile and re-proven.
(g) `ownerRecover` accepted with zero token-family inputs strands any note
still owned by the dissolved controller id (owner-only path; SDK includes the
position; recorded by the 2026-09-03 adversarial review).
