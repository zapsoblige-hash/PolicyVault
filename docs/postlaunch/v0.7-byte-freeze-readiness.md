# v0.7 ORGANIZATIONAL M-of-N OWNER ROOT — BYTE-FREEZE READINESS RECORD (stop packet, 2026-09-03)

**Verdict: V0.7-ORGANIZATIONAL-ROOT-TESTNET-VERIFIED-BYTE-FREEZE-READY.**

Meaning, exactly: the two candidate covenants below are VM-VERIFIED (real
TxScriptEngine on the candidate bytes), SDK/PRODUCTION-BYTE-VERIFIED (SDK-built
transactions driven through the real engine), EXPLAINED + SIGNER-PATH-TESTED +
FEE-MODELED at the shared-core boundary, and TESTNET-VERIFIED (a live
testnet-10 organizational lifecycle with chain verification, authorized
negative validation, and the consensus relative-age proof). They are **NOT
byte-frozen** (a later, deliberate owner/PM decision — never automatic), NOT
production, NOT mainnet. Frozen v0.5 and v0.6 are
untouched. Covenant generation v0.7 is unrelated to application release
numbers.

## 1. Identities

| item | value |
|---|---|
| root covenant `contracts/PolicyVault.v0.7-root.sil` (`PolicyVaultOrgRoot`) | sha256 `69417514f90d61b0281ea673e6fb5a5fd858c2bd50e14a4eb9043d5fdf17f8ce`; 542 lines; generator `tools/gen_v7_root.js` sha256 `dba4e37f79c893b24eb34893f78072eb0e70e0cf1ad502bdd10080bf48eb0763` (deterministic, `OUT=<path>`); redeem 11,545 B; state region 467 B with the fixed-width tail (`byte frozen` + `byte[8] rootNonce`, 11 B); 13 static sig-ops (12 counting at ONE call site + 1 succession) |
| rooted payment covenant `contracts/PolicyVault.v0.7-payment.sil` (`PolicyVaultRootedToken`) | sha256 `09cdbb6c284d8bd6c4cd2f4aad20d9682172eea3e048631176034b517be25091`; 677 lines; generator `tools/gen_v7_payment.js` sha256 `0d4d4008dcf0eeb65dd242764a81a352b3b63191f5e60a6f38a2cb6a3467817b` — reads the FROZEN v0.5 source, fails closed unless its sha256 is `c693aeff…`, applies 13 ordered exact-match edits each matching exactly once (+ 4 must-be-absent / 8 must-appear-once scans); redeem 9,636 B (+363 B vs frozen v0.5); 1 static sig-op |
| frozen priors (re-verified before and after every gate) | v0.5 `c693aeffb59286d21d44452bde0943d78840b66cf480b629624b7747b4197dd9`, v0.6 `c7c5f22c54a55d933ec8440a28bc2c628b9b99262541d50dbe9ffbdd16ba025c` |
| design record | `docs/postlaunch/v0.7-organizational-root-design.md` (§0 consensus facts … §15 live results) |
| encoder arms | `policyvault-0.7-root`, `policyvault-0.7-payment` (`tests/vm/src/bin/pv_call_encoder.rs`) |
| shared core | `core/model/{owner-set-v7,vault-state-v7-root,vault-state-v7,vault-transitions-v7-root,vault-transitions-v7,compute-budget-v7,fee-mass-v7}.js`; `core/intent/org-root-manifest-v7.js` (`policyvault-org-root-manifest/1`, `policyvault-rooted-vault-manifest/1`) + router; `core/explain/org-root-explain.js`; `core/signer/org-root-slot-v7.js` (`policyvault-org-root-slot-request/1` on the frozen signer v1 interface) |
| SDK | `sdk/src/contract-compiler-v7.js`, `sdk/src/vault-builders-v7.js` (root genesis; actions 0–4 + succession; rooted-vault genesis; rooted owner ops root+vault+fuel; delegate spend; deposit; M-of-N finalizer with the 65-byte/0x01 gate); `sdk/tools/gen-v7-vectors.js`, `capture-v7-manifests.js`, `measure-v7-fee-shapes.js`; `tools/v7-fee-curve.js`; `tools/testnet-v7-lifecycle.js` |
| live evidence | `docs/testnet-v7-org-root-evidence.json` + `docs/postlaunch/v0.7-testnet-evidence.md` (root id `9a654cb0…`, vault id `1f18d822…`, token family `8efcfa17…`) |
| economic record | `docs/postlaunch/v0.7-economic-viability.md` |

## 2. Gates (all green; every worker result re-run by the coordinator)

| gate | result |
|---|---|
| design → probe phase 1 (`v7_experiment_org_root.rs`) | 18/18; 80 engine refusals; the "few hundred bytes" delegate-cost claim FALSIFIED (+6,194 B) → phase 2; coordinator re-run 18/18 |
| probe phase 2 (`v7_experiment_org_root_p2.rs`, byte-level root-successor pinning) | 22/22; 97 engine refusals; delegate spend 9,636 B (target ≤ 10,600 B MET); coordinator re-run 22/22 |
| I1 VM production suites on the candidate bytes | `v7_root_production.rs` 17/17 (63 refusals), `v7_payment_production.rs` 18/18 (43 refusals); both drive the real `pv_call_encoder` binary output; measurement + standardness (static sig-ops ≤ 15, standard output classes, post-Toccata block-fit); stack headroom 83 / 142 / 117; coordinator re-run 17/17 + 18/18; generators regenerate BYTE-IDENTICALLY (coordinator) |
| I2 shared core + SDK + manifest + production bytes | `v7_sdk_integration.rs`: 39 SDK-built vectors (19 accept / 20 reject, each naming the failing input) + 50 SDK refusals + 10 verified manifests; 46 accept inputs executed within the SDK's committed budgets (every budget = §12.3); 93 tests; two product defects found + fixed (Node-only `Buffer` in the root digest; parent-action vs vault-op cross-check missing in the manifest); coordinator core 689, sdk v7 12/12, VM 4/4 |
| I3 explain / signer slot path / hostile matrix at the core boundary | 55 tests; every §4 field class refused before signing; two verifier defects found + fixed in the v0.7 verifier (rollover lockTime unbound; template pins unvalidated); I3C-F3: the FROZEN v0.5 manifest verifier's `rolloverLock` accepts an upward-forged lockTime (consensus refuses it) — fixed on the integration lane (defense-in-depth, availability class, not funds loss); coordinator core 739/739, sdk v7 18/18 |
| I4 fee/mass model | 31 shapes measured on finalized production bytes (every derived fee equals the SDK's committed fee): root AUTHORIZE 13,183 B / 0.02636600 KAS; rooted owner op 23,038 B / 0.04607600 KAS; ownerRecover 24,735 B / 0.04947000 KAS; delegate spend 12,280 B / 0.02456000 KAS (+0.000726 KAS, +3.0 % vs v0.5); threshold sweep built at every 1..12 (size constant through 9 owners; 12-of-12 +8.5 %); heartbeat ≈ 0.105 KAS/yr at a 90-day idle window |
| I5 LIVE testnet-10 | **19/19 CHAIN_VERIFIED** (issuance → root genesis → rooted vault genesis → deposit → delegated spend with no root input → root-governed setAgentRoot → emergency pause via FREEZE K=1 → unfreeze → unpause → ROTATE → threshold 2→3 → FREEZE → UNFREEZE 3-of-3 → heartbeat → OWNER-RECOVER → unfreeze → SUCCESSION → unfreeze → terminal vault recover to the genesis-pinned cold key across three owner-set generations); **consensus `check_sequence_lock` proven both ways with identical bytes** (OWNER-RECOVER rejected at age 49, accepted at 636; SUCCESSION rejected at 47, accepted at 624; node text `one of the transaction sequence locks conditions was not met`); **9/9 authorized testnet negative-validation transactions constructed independently of the PolicyVault application** rejected (invalid quorum M−1, wrong member, duplicate signature across slots, root successor swapped for a rotation inside a vault op, delegate over-cap 501 vs 500 → `script ran, but verification failed`; stale quorum vs the spent root outpoint → orphan; replay → already accepted); root value exactly 2.00000000 KAS across 11 transitions; coordinator independent node readback: final root UTXO `6626f2da…:1` = 200,000,000 sompi, genesis root address spent, recovery address holds `6626f2da…:0` = 299,950,000 sompi |
| final consolidated lane verification | core 739/739; sdk v7 suites 18/18; VM `v7_root_production` + `v7_payment_production` + `v7_sdk_integration` — see the continuation notes for the final run |

## 3. Rules adopted during the program (all probed)

D1 succession must install a different primary key (else its successor is byte-identical to FREEZE's) · D2 `require(periodLengthDaa > 0)` (closes the inherited v0.3–v0.6 F1 gap in the new generation) · D6 OWNER-RECOVER lands frozen like succession (a recovery transition is never byte-identical to AUTHORIZE; the installed set must deliberately unfreeze) · D7 int state fields are a constant 9 B · D8 byte-domain well-formedness for the fixed-width tail · D9 hoisted loop comparands (size only).

## 4. Security properties (CLAIM → ENFORCEMENT → TEST → EVIDENCE) — see `v0.7-security-claims.md`

## 5. Limitations and unresolved risks (honest)

1. Bytes NOT frozen;; no mainnet organization exists.
2. ONE rooted profile (the v0.5-derived token payment profile). Rooted KAS (v0.4.1) and composability (v0.6) profiles are future additive versions on the same mechanism; the hierarchical-delegation design must fit the rooted vault's remaining stack headroom.
3. The root UTXO serializes OWNER operations (one owner-op transaction per root transition); delegate spends are unaffected. Multi-vault batching in one transaction is permitted by the covenant and modelled by the manifest but NOT built by the SDK and NOT measured. **CORRECTION (2026-09-03, independent adversarial review probe 20, verified in source): multi-vault batching is REFUSED by each rooted vault's `requireOnlyRootCovenantInputs` (a sibling vault's non-zero covenant id is an unaccounted foreign rider) — one vault operation per root transition. The earlier sentence described the root's side only. Direction is safe (more restrictive; fails closed). See `v0.7-covenant-byte-freeze.md` §3–§4.**
4. Root template = 12 owner slots; larger sets are a different template/version.
5. `successorPk`, `recoveryDelayDaa`, `successionDelayDaa`, `rootMaxFeePerTx` are template constants (changing them = root migration); `recoveryM` is state.
6. Relative-age semantics are DAA-score based; owners keep recovery closed by heartbeating (AUTHORIZE) within the delay — an operational discipline the app must surface.
7. No app surface (server/web/MCP) for on-chain organizations yet; the web UI truthfully describes today's single on-chain owner.
8. The org-root manifest's slot-key binding at the portable-core boundary is a claim check (adapter `getPublicKey` vs the slot key) plus an optional injected verifier; the cryptographic slot↔key binding is the covenant's own `checkSig` (consensus).
9. The live proof used a 2-of-3 / 3-of-3 root with K=1, R=1, 600-DAA delays; owner sets above 3 active slots, ROTATE-while-frozen and multi-vault operations are engine-proven only.
10. Economic: an owner operation costs ≈ 0.046 KAS and a delegate spend +3.0 % vs v0.5 at the relay floor; no fee-market modelling beyond the floor.

## 6. What this record does NOT authorize

No byte freeze (a subsequent owner/PM decision), no production deployment, no publication of the candidate (v0.7 is NOT in the rc10 image or the v1.7.0 public candidate), no mainnet use, no hierarchical-delegation covenant. A byte freeze would follow the v0.5/v0.6 pattern: exact re-verification of both hashes and both regenerations, an independent adversarial review of the production bytes, and the permanent pin test.
