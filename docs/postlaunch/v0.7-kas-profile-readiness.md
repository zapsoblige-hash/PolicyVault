# v0.7-kas ROOTED KAS SAFE-PAYMENT PROFILE — READINESS RECORD (Wave 2, Track C, 2026-09-03)

**Status (UPDATED 2026-09-03, Track C2, same-day continuation): VM-VERIFIED
+ PRODUCTION-BYTE-VERIFIED (encoder AND the full SDK construction path) +
SDK-INTEGRATED + CROSS-RUNTIME-PORTABLE + CORE-BOUNDARY HOSTILE-MATRIX
PROVEN + TESTNET-10 LIFECYCLE TOOL DRY-RUN PROVEN. NOT byte-frozen, NOT
LIVE-testnet-verified (a local testnet-10 node is reachable but the
session's freshly-generated test key has zero balance — see §4 I5), NOT production.** This is the sibling of the rooted
PAYMENT profile (`docs/postlaunch/v0.7-byte-freeze-readiness.md`): the SAME
byte-level root-successor pinning mechanism (`requireRootAuthorization`,
the ROOT_TAIL constants, `requireOnlyRootCovenantInputs`), applied to the
FROZEN v0.4.1 plain-KAS covenant instead of the frozen v0.5 token
controller — so an organization gets covenant-level M-of-N ownership over
KAS treasury policy (per-agent caps, period budgets, a recipient Merkle
allowlist, and a vault-level M-of-N approval tier above a per-agent
threshold) without needing any token functionality.

This record's original body (§1-§8 below) was written by the FIRST Track C
worker (I1 done, I2 partial, I4 done, I6 written) and is preserved mostly
as-is; the **§0 UPDATE** immediately below records what the SECOND worker
(Track C2, continuing the same session-day) added on top of it: two
candidate hardening edits to the covenant (§0.1), then the rest of I2, I3,
and a --dry-run-proven I5. Read §0 first; it supersedes specific claims in
§1-§8 where noted.

## §0. UPDATE (Track C2) — candidate hardening + I2/I3/I5 completion

### §0.1 Two candidate hardening edits (covenant bytes CHANGED)

Before continuing I2, this session applied two exact-match generator edits
to `tools/gen_v7_kas.js` (E6), for parity with the already-proven
delegate-signature closure in the FROZEN `contracts/PolicyVault.v0.6.sil`:

1. **`requireAgentAuthorization(byte[] agentSig, pubkey agentPk)`** —
   `agentSpend`'s `agentSig` moves from an ungated `sig` parameter to a
   `byte[]` gated to exactly 65 bytes ending in `0x01` (SIG_HASH_ALL)
   before `checkSig` — the same A7 gate already applied to the 10
   approval slots.
2. **`requireOnlyPlainInputsOnDelegatePath`** — every input other than
   the vault itself must be a PLAIN input on `agentSpend`, refusing a
   root-covenant (or any other) rider on the delegate path
   unconditionally.

Both were extracted and applied **programmatically** against the pristine
frozen v0.4.1 text (mechanically verified exact-match, not
hand-transcribed) — see `tools/gen_v7_kas.js`'s E6 block and its inline
comments for the exact mechanism. The two "documented inherited behavior"
rows in the ORIGINAL §6 below (root rider accepted; non-ALL agent sighash
accepted) are consequently **STALE for the current candidate bytes** — a
root-covenant rider and a non-SIG_HASH_ALL / forged-gate-byte agent
signature are now REFUSED, not accepted. §6 is left unedited as the
historical record of the pre-hardening candidate; this note is the
correction.

**New identities (supersede §1 below):**

| item | value |
|---|---|
| covenant `contracts/PolicyVault.v0.7-kas.sil` sha256 | `393f2130b48d06ba4d5b29ad8846098dd16ae89ebfd658a2df163f0801ac1d8b` (was `7155f9e7…`); 1,089 lines (was 1,044) |
| generator `tools/gen_v7_kas.js` sha256 | `1cd3d30be883afb338ac156c4bb9c983b198e954e27c4e0ef1a5769a161a7014` (was `ec54eb90…`); 7 exact-match edits (was 5) |
| frozen priors re-verified unchanged (before AND after E6) | v0.4.1 `421bfed8…`, v0.5 `c693aeff…`, v0.6 `c7c5f22c…`, v0.7-root `69417514…`, v0.7-payment `09cdbb6c…` |

**Re-measured (release build, real TxScriptEngine), confirming E6 did not
regress standardness or stack safety:**

| shape | before E6 | after E6 |
|---|---|---|
| static P2SH sig-ops (vault redeem) | 11 | 11 (unchanged) |
| vault redeem | 18,651 B | 18,941 B (+290 B) |
| basic delegate spend sigscript | 19,929 B | 20,219 B (+290 B) |
| MAX_STACK_SIZE (244) headroom, basic delegate spend | 84 (peak 160) | 78 (peak 166) |
| relay-floor fee, every shape (MINIMAL budgets) | — | up ~0.0006 KAS uniformly (negligible) |

`tests/vm/tests/v7_kas_production.rs` grew from 30/30 to **34/34 passed**
(release, ~80s): the two prior accept rows became refusals, plus a
forged-gate-byte refusal and two positive controls.
`tests/vm/tests/v7_kas_encoder_integration.rs` grew from 5/5 to **7/7
passed** (debug, ~17.5s): the same pattern via the compiled
`pv_call_encoder` binary.

### §0.2 I2 completed (was PARTIAL — now DONE)

- `sdk/src/contract-compiler-v7-kas.js` — exact live-state compiler.
  Reuses `buildLiveStateSourceV4` (`sdk/src/contract-compiler-v4.js`)
  VERBATIM for the state-templating swap (every anchor line is carried
  byte-for-byte from frozen v0.4.1 by the E1-E6 delta), adding only the
  new 21-argument constructor shape. Reuses
  `compileExactStateV7Root`/`deriveRootPinsV7` from
  `sdk/src/contract-compiler-v7.js` unmodified for the root side.
- `sdk/src/vault-builders-v7-kas.js` — genesis + build/finalize for
  `agentSpend` (both funding modes: RESERVE and FUEL; both approval
  regimes: at/below and above the agent's `approvalThreshold`, via the
  frozen v0.4.1 vault-level M-of-N approver tier, reusing
  `sdk/src/approval-package-v4.js` verbatim) and all 7 `ownerControl`
  selectors + `ownerRecover`. **Root-only genesis/rotation and the M-of-N
  root finalizer are imported and RE-EXPORTED from
  `sdk/src/vault-builders-v7.js`, never re-implemented**, per this
  track's instructions.
- `core/intent/org-root-manifest-v7-kas.js` — two manifest families:
  `policyvault-org-root-kas-manifest/1` (owner ops embedded, mirroring
  `core/intent/org-root-manifest-v7.js`'s pattern in a SEPARATE module
  since that file's `vaultOperations` loop is hard-wired to the payment
  profile) and `policyvault-rooted-kas-vault-manifest/1` (delegate
  spends standalone). The vault-level M-of-N approver tier is shown as
  **counts + public keys + a package commitment digest, never raw
  signatures**.
- `core/intent/router.js` — both families registered additively
  (`MANIFEST_FAMILIES` and `ROUTES["policyvault-0.7-kas"]`); the
  existing closed-set pin tests (`core/intent/test/router.test.js`) were
  deliberately extended, every prior test stays green (101/101 in
  `core/intent/test`).
- **Production-byte proof**: `sdk/tools/gen-v7-kas-vectors.js` drives the
  real SDK + real `pv_call_encoder` + real Schnorr signing to build 28
  fully-finalized transactions (16 accept / 12 reject), 15 SDK
  pre-build/pre-finalize refusals, and 9 manifest verifications;
  `tests/vm/tests/v7_kas_sdk_integration.rs` executes every emitted
  vector's EXACT bytes on the real TxScriptEngine — **5/5 passed**
  (release, ~144s).
- **Cross-runtime portability**: `core/crossruntime/test/v7-kas-portability.test.js`
  proves the new core/model modules (and the frozen v0.4.1 modules they
  delegate to) execute identically in Node and a browser-like V8 sandbox
  — **23/23 passed**. Scope note: the intent/manifest layer is NOT
  covered here, matching the payment profile's own precedent (its
  `org-root-manifest-v7.js` isn't covered by any crossruntime test
  either).
- Two PRE-EXISTING defects (from the FIRST worker's I2a commit, not this
  session's E6 work) were found and fixed as a side effect of this
  proof: `core/model/test/purity.test.js`'s closed-set file pin never
  listed the three new `core/model` files, and
  `core/crossruntime/test/v5-v6-portability.test.js`'s sandbox file list
  did not include the router's new dependency — both fixed; full
  `core/model` (169/169) and `core/crossruntime` (267/267) suites green.

**NOT done in I2**: the `core/explain/` signer-rendering extension for
this profile (presentation-layer only; not required for the
production-byte proof; honestly recorded as a gap in §7).

### §0.3 I3 completed (was NOT DONE — now DONE)

`sdk/test/rooted-kas-hostile-matrix.test.js`: every scoped manifest field
class has a mutated sibling of a real SDK-built manifest REFUSED with the
failing check named, BEFORE anyone signs — **9 rows across 2 tests, both
passed**: root pins, template pins, parent-action cross-check (×2),
recovery redirect, amounts, recipient, lockTime binding, and the
vault-level approver tier. Scoped to exactly the 7 field classes this
track's I3 assignment named (a smaller, more targeted matrix than the
payment profile's own 3-arm ~30-row suite, which additionally covers ARM
1 sanity-of-every-shape and ARM 3 cross-checking all 20 production-byte
reject vectors — not attempted here for time).

### §0.4 I5 completed to DRY-RUN PROVEN (was NOT ATTEMPTED — live NOT done)

`tools/testnet-v7-kas-lifecycle.js`: the full lifecycle (root genesis →
rooted KAS vault genesis → `ownerTopUpReserve` as the funding-after-genesis
analog → `ownerSetApprovers` → delegate spend below threshold → delegate
spend above threshold with 2-of-3 vault-level approvals → `ownerSetAgentRoot`
→ EMERGENCY pause under root FREEZE (K=1) → governed unfreeze → `ownerUnpause`
→ 6 authorized negative-validation rows constructed independently of the
SDK builders → terminal `ownerRecover`) runs cleanly under `--dry-run`:
**44 steps recorded, 5 of 6 negatives fired** (the 6th, replay-of-accepted-
bytes, is legitimately skipped in synthetic dry-run mode — there is no
prior ACCEPTED on-chain transaction to replay against without a real
broadcast, the same conditional the payment profile's own lifecycle tool
uses). The LIVE-run serialization lock protocol
(`~/.policyvault-testnet-live.lock`) is implemented and
NEVER acquired in `--dry-run`.

**Live execution was NOT attempted this session** — not merely deferred
for time, but genuinely blocked: a local testnet-10 node IS confirmed
running and reachable (`ws://127.0.0.1:18210`), but this worktree's test
key file (`keys/policyvault_test_keys.json`, gitignored, freshly generated
by `loadOrCreateTestKeys` on first use) has a zero on-chain balance, no
testnet faucet was reachable/attempted this session, and borrowing funded
key material from another lane's worktree is out of scope
("never touch other worktrees"). The exact next action: fund this
worktree's `owner` test key address on testnet-10 (via faucet or an
owner-directed transfer from an already-funded key), then run
`KASPA_NETWORK_ID=testnet-10 KASPA_RPC_URL=ws://127.0.0.1:18210 node
tools/testnet-v7-kas-lifecycle.js` (no `--dry-run` flag) from this
worktree.

Two real bugs were found and fixed by the dry run before any live use
(both purely in the NEW lifecycle-tool script, not in the SDK/core layers
it drives): (1) the initial delegate-spend amount constants were mutually
inconsistent (a spend above the approver threshold also exceeded the
per-spend cap; the vault's protected principal could not cover the sum of
the honest spends) — retuned; (2) `normalizeStateV4` CANONICALIZES
(lexicographically sorts) the vault-level approver slots rather than
preserving caller-supplied order, so the hand-rolled "approval shortfall"
negative-validation transaction must read back the REAL slot index
`submitApprovalV4` assigned (`pkg.approverSlots.indexOf(...)`) rather than
assuming `approverKeys[i]` lands at slot `i` — fixed by building the blob
from `pkg.approvals` directly. This is a useful, previously-undocumented-
in-this-track fact about the shared v4 core worth carrying forward: **the
vault-level M-of-N approver tier's slot ORDER is not caller-controlled.**

---

### §0.5 rc26 round-7 internal review (2026-09-06) — R7-04 recorded as a PROFILE BLOCKER; R7-01 / R7-02 parity applied

- **R7-04 (INFO in the rc26 review; a BLOCKER for this profile's own readiness):** the rooted-KAS candidate verifier
  (`core/intent/org-root-manifest-v7-kas.js`) has NO shared-core pin / successor-script reconstruction (`vaultGenerationAgrees`
  states it): a substituted vault successor script, a substituted `recoveryPk` + payout, or a substituted vault input
  sequence VERIFY at the kas verifier (round-7 probe P4; `docs/postlaunch/audit-evidence/rc26-internal-review/`). Today
  this cannot reach a signer: the profile is UNFROZEN, never mainnet-creatable, has no browser initiation or signing route for its requests (the browser refuses with 0 wallet calls). RECORD CORRECTION (Codex checkpoint 11): the generic router DOES register `ORG_ROOT_KAS_MANIFEST_VERSION_1` (`core/intent/router.js`); the round-7 statement "router `UNKNOWN_VERSION`" came from a probe that passed a manifest-version string where a contract version was expected — see `docs/postlaunch/audit-evidence/rc26-internal-review/ERRATA.md`. It is recorded here as a condition of ANY future enablement
  of this profile: a `vault-script-v7-kas` skeleton (the v0.7-kas analogue of `core/intent/vault-script-v7.js`) binding
  the declared pins and the reviewed predecessor state to the vault the transaction actually spends, with the same
  RED-first hostile matrix the payment profile carries. NOT built in the rc27 correction wave (the profile is outside the
  release scope); nothing about the profile's status changes (still candidate, still testnet-only, still not frozen).
- **R7-01 / R7-02 parity APPLIED on rc27:** the kas verifier now binds every non-root input sequence (`inputSequencesZero`),
  the non-age-gated root input sequence (`rootInputSequenceZero`) and the lockTime (`lockTimeZero`, inner + outer), and the
  kas `ownerSetAgentRoot` manifest carries the new (v4-layout) policy set bound to `stateAfter.agentRoot`
  (`agentSetPresent` / `agentSetBound`; the kas builder requires `params.agents`) — `sdk/test/rooted-kas-hostile-matrix.test.js`
  (real builds, RED on cf4e644).

## ORIGINAL RECORD (first Track C worker, unedited below except where §0 above notes a supersession)

## 1. Identities

**STALE — see §0.1 for the current (post-E6) covenant/generator sha256 and
line counts.** Everything else in this table (root pin, encoder arm, VM
production suite NAME, shared core module NAMES) is still accurate; only
the two hashes and line counts changed.

| item | value |
|---|---|
| covenant `contracts/PolicyVault.v0.7-kas.sil` (`PolicyVaultRootedKas`) | sha256 `7155f9e75303ced91f2141716ff1757ab8a096d3f7c23ea66b07668ecf1ff150`; 1,044 lines |
| generator `tools/gen_v7_kas.js` | sha256 `ec54eb90d6617b8487b28997c246115c335cd9b9b57e6f6bdc9d824bcb75f9f7`; reads the FROZEN `contracts/PolicyVault.v0.4.1.sil`, fails closed unless its sha256 is exactly `421bfed824cf66a9e989f90c5b86fc7359faa070a5d94aace3c325f35ad1da4e`, applies 6 ordered exact-match edits (each matching exactly once) + must-be-absent (4 needles) / must-appear (9 needles) scans; regenerates BYTE-IDENTICALLY (verified) |
| base (read-only, never modified) | `contracts/PolicyVault.v0.4.1.sil` sha256 `421bfed824cf66a9e989f90c5b86fc7359faa070a5d94aace3c325f35ad1da4e` |
| shared root (read-only, unchanged) | `contracts/PolicyVault.v0.7-root.sil` sha256 `69417514f90d61b0281ea673e6fb5a5fd858c2bd50e14a4eb9043d5fdf17f8ce` |
| frozen priors re-verified unchanged (before AND after this work) | v0.4.1 `421bfed8…`, v0.5 `c693aeff…`, v0.6 `c7c5f22c…`, v0.7-root `69417514…`, v0.7-payment `09cdbb6c…` |
| encoder arm | `policyvault-0.7-kas` added to `tests/vm/src/bin/pv_call_encoder.rs` — PURELY ADDITIVE (the only line touched in the whole file outside the new arm is the `v07` signature-exemption `matches!` set, extended to include the new version tag; `git diff` confirms every existing arm is byte-identical) |
| VM production suite | `tests/vm/tests/v7_kas_production.rs` — 30/30 passed (release, ~94s) |
| VM production-byte (encoder) suite | `tests/vm/tests/v7_kas_encoder_integration.rs` — 5/5 passed (debug, ~18s), drives the COMPILED `pv_call_encoder` binary, never the in-process library encoder |
| shared core (new) | `core/model/vault-state-v7-kas.js`, `core/model/vault-transitions-v7-kas.js`, `core/model/compute-budget-v7-kas.js` — DELEGATE the mutable-state/transition math to `core/model/vault-state-v4.js` / `vault-transitions-v4.js` (byte-for-byte v0.4.1), adding only the root-authority overlay; reuse `core/model/compute-budget-v7.js`'s `selectRootComputeBudgetV7` and `ROOT_TEMPLATE_REFERENCE_BYTES` UNCHANGED for the root side |
| economic record | `docs/postlaunch/v0.7-kas-economic-viability.md` |
| this record | `docs/postlaunch/v0.7-kas-profile-readiness.md` |

## 2. What changed vs the frozen v0.4.1 base, and only this

- `pubkey owner` REMOVED. No owner key, no owner signature anywhere.
- Added template constants: `orgRootCovenantId`, `rootTemplateVmHash`,
  `rootPrefixLen`, `rootStateLen`, `rootSuffixLen`, `recoveryPk` (the
  genesis-pinned cold recovery destination).
- `requireRootAuthorization` + `requireOnlyRootCovenantInputs` inserted
  verbatim (identical text to the payment profile's) after the state field
  declarations.
- `require(periodLengthDaa > 0)` (D2) added at the top of
  `requireAgentTransition` — the v0.6 adversarial-review hardening the
  frozen v0.3–v0.6 covenants lack, now present here as in the payment
  profile.
- `ownerControl` MERGES v0.4.1's six selectors (0 setAgentRoot,
  1 setApprovers, 2 topUp, 3 topUpReserve, 4 pause, 5 unpause — all full
  quorum / AUTHORIZE) with a NEW selector 6 (EMERGENCY pause, the lighter
  emergency quorum / FREEZE) behind ONE bounded selector 0..6, one inline
  `requireRootAuthorization` call site.
- `ownerRecover` pays `protectedValue + feeReserve` to the genesis-pinned
  `recoveryPk` under the root's FULL quorum (AUTHORIZE); no token
  continuation (this profile is plain KAS).
- Every v0.4.1 delegate rule is carried BYTE-FOR-BYTE and unchanged:
  per-spend cap, period budget + rollover (CLTV-gated), recipient Merkle
  allowlist, the vault-level M-of-N approver tier above a per-agent
  threshold (A2/A7 gates), the agent Merkle set (single-leaf update,
  depth ≤ 12), fee-reserve conservation (FR-1), exact successor value,
  pause.

## 3. Measurements (real TxScriptEngine, release build)

| shape | value | source |
|---|---|---|
| static P2SH sig-ops (vault redeem) | **11** (v0.4.1 base was 13 — the two removed owner checkSigs) | `post_toccata_p2sh_sig_scanner` on a real agentSpend sigscript, asserted `== 11` |
| vault redeem | 18,651 B | `v7kas_honest_owner_control_selectors_0_to_6` |
| root redeem | 11,545 B (unchanged root covenant) | same |
| delegate spend (basic) redeem/sigscript | 18,651 B / 19,929 B | `v7kas_honest_delegate_spend` |
| delegate spend at production proof depths (agent 12 / recipient 16) | sigscript 20,829 B, units 173,829 | `v7kas_honest_delegate_spend_at_production_proof_depths` |
| MAX_STACK_SIZE (244) headroom, basic delegate spend | peak combined stack 160, headroom 84 | `v7kas_measurement_standardness_and_stack` |
| worst case (agent depth 12 + recipient depth 16 + 10-of-10 approvals), unpriced | compute_units 217,007 | `v7kas_worst_case_stack_and_execute` |
| same worst case, PRICED (100,000 units/executed sig-op; 11 sig-ops execute) | 1,317,007 units, required budget 131 | `v7kas_measure_priced_worst_case_delegate_spend` |
| ownerControl(0..6) priced units (vault input) | 359,348 .. 364,474 (max = selector 1, setApprovers) | `v7kas_measurement_standardness_and_stack` |
| ownerRecover priced units (vault input) | 278,188 | same |
| relay-floor fee, ownerControl(0/6) + root, MINIMAL budgets | **0.06443800 / 0.06443600 KAS** | `v7kas_economic_viability_minimal_budgets` |
| relay-floor fee, ownerRecover + root, MINIMAL budgets | 0.06357800 KAS | same |
| relay-floor fee, delegate spend basic, MINIMAL budget | **0.04043200 KAS** | same |
| relay-floor fee, delegate spend depth 12/16, MINIMAL budget | 0.04223200 KAS | same |

Full detail and the comparison against the rooted payment profile:
`docs/postlaunch/v0.7-kas-economic-viability.md`.

## 4. Gates — what is done, and the I1–I6 discipline this record follows honestly

| gate | status | evidence |
|---|---|---|
| I1 covenant candidate + VM production suite | **DONE** (I1) **+ HARDENED** (Track C2 §0.1, E6). 34/34 (`v7_kas_production.rs`, up from 30/30): honest paths, the original 40-row hostile matrix, D6 rows, delegate-side named categories, measurement/standardness/stack, priced worst-case + minimal-budget economics, PLUS the E6 rows (root-rider refusal + positive control, non-ALL-sighash refusal, forged-gate-byte refusal + positive control). Frozen v0.4.1/v0.5/v0.6 and candidate v0.7-root/v0.7-payment hashes verified unchanged before and after E6. | `tests/vm/tests/v7_kas_production.rs`, `tests/vm/tests/v7_kas_encoder_integration.rs` (7/7) |
| I2 shared core + SDK + manifest + production bytes | **DONE** (Track C2 §0.2, was PARTIAL). `sdk/src/contract-compiler-v7-kas.js`, `sdk/src/vault-builders-v7-kas.js` (root-only ops re-exported from `vault-builders-v7.js`, never re-implemented), `core/intent/org-root-manifest-v7-kas.js` (two manifest families), `core/intent/router.js` registration, `sdk/tools/gen-v7-kas-vectors.js` -> `tests/vm/tests/v7_kas_sdk_integration.rs` (5/5: 28 vectors, 15 refusals, 9 manifests), `core/crossruntime/test/v7-kas-portability.test.js` (23/23). **NOT DONE:** the `core/explain/` signer-rendering extension (presentation-layer only; see §7). | §0.2; `tests/vm/tests/v7_kas_sdk_integration.rs`; `core/crossruntime/test/v7-kas-portability.test.js` |
| I3 hostile matrix at the core boundary | **DONE** (Track C2 §0.3, was NOT DONE), scoped to the 7 field classes this track named: amounts, recipient, approvals, root pins, template pins, lockTime binding, parent-action cross-check — 9 rows, 2/2 tests passed. Narrower than the payment profile's own 3-arm suite (no ARM-1 every-shape sanity sweep, no ARM-3 all-20-reject-vectors cross-check) — an honest scope cut, not an oversight. | §0.3; `sdk/test/rooted-kas-hostile-matrix.test.js` |
| I4 fee/mass economic viability | **DONE.** `docs/postlaunch/v0.7-kas-economic-viability.md`, real measurements at MINIMAL required budgets, compared against the payment profile with an explained (approver-machinery) cost delta. Fees shifted up ~0.0006 KAS uniformly after E6 (negligible); not re-published as a separate document, recorded in §0.1's re-measurement table instead. |
| I5 LIVE testnet-10 lifecycle | **DRY-RUN PROVEN** (Track C2 §0.4, was NOT DONE); **LIVE NOT DONE** — genuinely blocked on test-key funding this session, not merely deferred (see §0.4 for the exact reason and next action). `tools/testnet-v7-kas-lifecycle.js --dry-run`: 44 steps, 5/6 negatives (replay legitimately requires a real prior broadcast). The local testnet-10 node (`ws://127.0.0.1:18210`) IS confirmed running and reachable; the serialization lock (`~/.policyvault-testnet-live.lock`) was never acquired since no live broadcast was attempted. | §0.4; `tools/testnet-v7-kas-lifecycle.js` |
| I6 readiness record | **DONE** — this document (§0 added by Track C2; §1-§8 below are the first worker's original record, preserved). |

## 5. Security claims (summary; CLAIM → ENFORCEMENT → TEST → EVIDENCE)

See the added rows in `docs/postlaunch/v0.7-security-claims.md` for the full
table. In short, PROVEN at VM + production-byte-encoder level (not yet
testnet-verified):

1. Owner authority is the pinned organizational root input, never a key —
   same byte-level pin as the payment profile, reused verbatim.
2. `ownerControl` selectors 0-5 require the root's FULL quorum (AUTHORIZE);
   selector 6 (EMERGENCY pause) is the ONLY vault effect reachable from the
   lighter emergency quorum (FREEZE) and is AUTHORITY-REDUCING/monotone.
3. Recovery cannot be redirected: `ownerRecover` pays the GENESIS-PINNED
   `recoveryPk`, under the root's FULL quorum.
4. Every v0.4.1 delegate protection is retained; the delegate path never
   touches the root (VM-proven, plus an explicit accepted-and-documented
   exception below).
5. D2 (`periodLengthDaa > 0`) closes the same vacuous-period gap already
   closed in the payment profile.
6. Standardness: 11 static sig-ops (≤ 15), confirmed by the real scanner
   (unchanged after §0.1's E6 hardening).
7. **(Track C2, §0.1) `agentSpend`'s own signature is now gated to
   SIG_HASH_ALL, and the delegate path refuses any covenant rider
   (including a root-covenant rider) — CANDIDATE-HARDENED, superseding
   claims 1/2 of §6 below.**

## 6. Two measured, investigated, and classified behavioral differences from the payment profile (NOT defects)

**STALE for the current candidate bytes — see §0.1.** Both rows below were
true of the covenant BEFORE Track C2's E6 hardening; the covenant now
REFUSES both. Kept verbatim as the historical record of what the first
worker measured and why it was judged safe even before the hardening.

1. **`agentSpend` accepts a root-covenant rider.** Unlike the payment
   profile (which inherits v0.5's `requireNoForeignCovenantInputs` on
   `tokenAgentSpend`), v0.4.1's `agentSpend` has NO foreign-covenant-family
   check at all — it predates the concept of multiple covenant families
   coexisting in one PolicyVault transaction. Verified SAFE: `agentSpend`'s
   fee/principal/successor rules are entirely self-bound via
   `OpAuthOutputIdx(this.activeInputIndex, 0)` and do not depend on what
   else the transaction contains. Test:
   `v7kas_delegate_spend_accepts_a_root_covenant_rider_documented_inherited_behavior`.
2. **A non-`SIG_HASH_ALL` agent signature is accepted.** Unlike the 10
   approval slots (explicit A7 gate,
   `require(approvalHashByte == bytes(0x01))` before `checkSig`), the
   agent's own `checkSig(agentSig, agentPk)` carries no sighash-type gate in
   either the frozen v0.4.1 base or this delta (one call site each,
   grep-verified). Verified SAFE: `requireAgentRecipient` and
   `requireExactSuccessorValue` bind `tx.outputs[]` STRUCTURALLY — the VM
   reads the actual executing transaction's outputs directly, independent of
   what the signature's sighash type covers — so a non-ALL agent signature
   cannot be used to redirect value. Test:
   `v7kasenc_non_all_agent_sighash_is_accepted_documented_inherited_behavior`
   (initially asserted the opposite and wrong expectation, caught by the VM
   disagreeing with the test; corrected and classified TEST BUG, not a
   contract bug).

## 7. Limitations and unresolved risks (honest)

**Items 2 and 3 below are STALE — corrected by Track C2's §0; left
unedited here as the historical record of the first worker's honest gap
list, with the correction inline.**

1. **Bytes NOT frozen.** No byte freeze was requested or granted; this is a
   CANDIDATE (this remains true after §0's E6 hardening — E6 is a bytes
   CHANGE, not a freeze). Owner/PM authorization for a byte freeze is a
   separate, deliberate, never-automatic step (matching the
   v0.5/v0.6/v0.7-payment precedent: exact re-verification of both hashes
   and regenerations, an independent adversarial review, and a permanent
   pin test).
2. ~~**NOT testnet-verified.**~~ **CORRECTED (§0.4): DRY-RUN PROVEN, LIVE
   testnet-10 execution still NOT done** (genuinely blocked on test-key
   funding this session, not merely deferred — see §0.4 for the exact next
   action).
3. ~~**NOT SDK-integrated.**~~ **CORRECTED (§0.2): the SDK transaction-
   building layer now EXISTS** (`sdk/src/vault-builders-v7-kas.js` +
   `sdk/src/contract-compiler-v7-kas.js`), is production-byte proven
   end-to-end, and a real wallet/API integration COULD use this profile
   today — it has simply never moved a transaction on a live node.
4.**. NOT production.**
5. **Vault redeem is roughly double the payment profile's** (18,941 B as
   of §0.1's E6 hardening, up from 18,651 B; the payment profile is
   9,636 B) because this profile carries the full v0.4.1 vault-level
   10-approver-slot M-of-N machinery. An organization that does not need
   that tier still pays for its PRESENCE in the redeem (SilverScript inlines
   every function at every call site) but not for its EXECUTION when unused
   — see the economic-viability record.
6. **The root side of a combined transaction is UNCHANGED and re-uses
   `contracts/PolicyVault.v0.7-root.sil` and its existing `pv_call_encoder`
   arm verbatim** — this record does not re-derive root-side security
   claims; see `docs/postlaunch/v0.7-byte-freeze-readiness.md` §4 and
   `docs/postlaunch/v0.7-security-claims.md` for those.
7. **Rooted vault genesis, and a delegate spend that actually exercises the
   vault-level approval tier, were not separately mass-measured** — only the
   sig-op-priced worst case anchors that cost (§3). §0.4's dry-run DOES now
   exercise both shapes functionally (build/sign/finalize), just not with a
   dedicated mass measurement.
8. **Only the VAULT side of this profile's redeem is new; the requireRootAuthorization /
   requireOnlyRootCovenantInputs text is copied verbatim from the payment
   profile**, not independently re-derived — any future defect found in that
   shared logic (e.g. via the payment profile's own adversarial review)
   applies equally here and must be re-checked against this file too. The
   §0.1 `requireAgentAuthorization` / `requireOnlyPlainInputsOnDelegatePath`
   additions are NOT shared with the payment profile (new code, mirroring
   v0.6's pattern) and have their own, narrower proof surface (§0.1).
9. **(NEW, Track C2) `core/explain/` was not extended for this profile.**
   A signer today reads the raw manifest JSON; there is no human-readable
   rendering layer for v0.7-kas yet (the payment profile's own
   `core/explain/org-root-explain.js` does not cover it either).
10. **(NEW, Track C2) I3's hostile matrix is scoped, not exhaustive** — 7
    field classes as assigned, not the payment profile's full 3-arm
    ~30-row suite (see §0.3).

## 8. What this record does NOT authorize

No byte freeze. No production deployment. No mainnet use. No claim of
"audited." No claim that this profile has ever
moved value on a live node — I5 is DRY-RUN PROVEN only (§0.4). The
covenant is CANDIDATE; it is VM-VERIFIED, PRODUCTION-BYTE-VERIFIED (both
the call-encoder AND the full SDK construction path), SDK-INTEGRATED,
CROSS-RUNTIME-PORTABLE, and core-boundary-hostile-matrix-proven (§0), but
NOT live-testnet-verified and NOT frozen.

## Post-record note — I2 production-byte proof re-established (2026-09-09, [CLAUDE], rc29 continuation)

The I2 claim "`tests/vm/tests/v7_kas_sdk_integration.rs` 5/5" above dates from `0e5902c`. Between the R7-02 builder correction (`9da991f`, 2026-09-07) and 2026-09-09 the generator `sdk/tools/gen-v7-kas-vectors.js` was stale (bare `newAgentRoot` refused with `AGENT_SET_REQUIRED`), so the suite was RED for that period without being re-run. The generator now passes the full delegate policy set with its recipient set (no builder or covenant byte changed) and the suite passes again on the real engine from the public v1.9.1 tree: **5/5** (28 vectors: 16 accept / 12 reject, 15 SDK refusals, 9 manifest checks; `docs/postlaunch/ux-evidence/claude-rc29/repro-v191/delta-vm/`). The profile remains an unfrozen, testnet-only candidate that is NOT mainnet-creatable.
